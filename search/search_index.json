{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"Sucata <p>     Gather yours scraps and recycle into a game!  </p>  Installation   Creating your first project   References   Examples   Repository"},{"location":"#overview","title":"Overview","text":"<p>Sucata game engine is a 2D game engine made to be simple and easy to use, with a lot of features to make your game development experience better.</p> <p>The goal is to make a create game experience similar good and smooth using only Lua scripting language.</p>"},{"location":"#objective","title":"Objective","text":"<p>Create a game framework that is simple to use, with a simple workflow, and lightweight, for developing simple games.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Render images and texts with GPU acceleration (using native libraries like D11X, Metal, OpenGL)</li> <li>Audio playback and manipulation</li> <li>Input handling (keyboard, mouse)</li> <li>Gamepad support</li> <li>Scene management</li> <li>Lua scripting</li> <li>Cross-platform support (Windows, macOS, Linux)</li> <li>Simple and intuitive file system for asset management</li> <li>Simple Custom Shader support</li> </ul>"},{"location":"#future-plans","title":"Future plans","text":"<ul> <li>More animation features</li> <li>Network multiplayer support</li> <li>Particle system</li> <li>Maybe 3D support in the future</li> <li>Support with Tiled</li> <li>Native call support for C/C++ functions</li> </ul>"},{"location":"#special-thanks","title":"Special Thanks","text":"<ul> <li>Ellora</li> <li>ThornDuck</li> <li>Randy</li> </ul>"},{"location":"about/","title":"About","text":"<p>Sucata game engine is a 2D game engine made to be simple and easy to use, with a lot of features to make your game development experience better.</p> <p>The goal is to make a create game experience similar good and smooth using only Lua scripting language.</p>"},{"location":"about/#contributors","title":"Contributors","text":"<ul> <li>Gustavo Maia Paes (gump) - Creator</li> <li>Alexander Geraldine - Researcher and Documenter</li> <li>Ellora - Computer Graphics Consulting</li> <li>Murilo M. Grosso (ThornDuck) - Testing and icon creator</li> </ul>"},{"location":"agent-guide/","title":"Building a game with Sucata (Agent Guide)","text":"<p>This page is a practical, self-contained guide for creating a game with Sucata, in the spirit of Love2D/Godot. It's aimed at whoever \u2014 human or AI coding agent \u2014 is writing a game on top of Sucata, condensing everything needed to be productive on one page, with links to the full reference for exhaustive per-function detail. It's the same guide shipped as <code>AGENT.md</code> in the sucata repository, for use by coding agents working directly in a project's source tree.</p>"},{"location":"agent-guide/#what-sucata-is","title":"What Sucata is","text":"<ul> <li>Engine core: Odin, using sokol for graphics/audio-input plumbing, SDL3 for gamepad, miniaudio for audio.</li> <li>Game logic: pure Lua. You never touch Odin to make a game \u2014 only to contribute to the engine itself.</li> <li>Everything is data + functions. There is no class/inheritance system and no scene-graph hierarchy. A game is a flat pool of entities (state + a list of reusable behaviours) plus global systems (input, audio, timers, events) you call from Lua.</li> </ul>"},{"location":"agent-guide/#setup","title":"Setup","text":"<pre><code># install \u2014 see the installation guide for the one-liner per OS\nsucata version   # verify install\n</code></pre> <p>Recommended: install the sumneko Lua extension + the sucata Lua addon (Addon Manager \u2192 search \"sucata\") for autocompletion against the engine's Lua API.</p>"},{"location":"agent-guide/#cli-commands","title":"CLI commands","text":"Command Purpose <code>sucata run &lt;main.lua&gt; [--entity &lt;file&gt;]</code> Run a game (or a single entity file, for isolated testing) through the engine in dev mode. <code>sucata build &lt;main.lua&gt; [--icon &lt;path&gt;]</code> Package the game into a native, OS-specific distributable (see \"Building\" below). Current OS only, no cross-build yet. <code>sucata shader build &lt;file.glsl&gt;</code> Compile a custom <code>.glsl</code> shader into the engine's <code>.schd</code> format. <code>sucata shader create &lt;file&gt; [--post-processing\\|-pp] [--font\\|-f]</code> Scaffold a starter <code>.glsl</code> shader template. <code>sucata version</code> Print engine version. <p>See the CLI page for full flag details.</p>"},{"location":"agent-guide/#project-layout-convention","title":"Project layout convention","text":"<p>There's no enforced scaffold, but every existing Sucata game (including the official meteors-sucata example, and the First Project tutorial) follows this shape:</p> <pre><code>my-game/\n\u251c\u2500\u2500 main.lua              Entry point \u2014 required first by `sucata run`/`sucata build`\n\u251c\u2500\u2500 config.lua             Window setup (see below) \u2014 required from main.lua\n\u251c\u2500\u2500 behaviours/\n\u2502   \u251c\u2500\u2500 init.lua           Aggregates ALL behaviours into one shared table (see \"Behaviours\" below \u2014 important!)\n\u2502   \u251c\u2500\u2500 player.lua\n\u2502   \u251c\u2500\u2500 meteor.lua\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 entities/              Factory functions: fn(...) -&gt; { state = {...}, behaviours = {...} }\n\u2502   \u251c\u2500\u2500 player.lua\n\u2502   \u251c\u2500\u2500 meteor.lua\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 states/                Optional: plain modules of pure functions that mutate a state field\n\u2502   \u2514\u2500\u2500 health.lua         (shared logic callable from multiple behaviours, e.g. health.remove(state))\n\u251c\u2500\u2500 sprites/                Texture assets (.png)\n\u251c\u2500\u2500 sounds/                 Audio assets (.ogg, etc)\n\u2514\u2500\u2500 fonts/                  Font assets\n</code></pre> <p><code>main.lua</code> example:</p> <pre><code>require(\"config\")\n\nBehaviours = require(\"behaviours\")  -- single shared reference, see below\n\nlocal Player = require(\"entities.player\")\nlocal game_manager = require(\"entities.game_manager\")\n\nsucata.scene.spawn(Player(480, 500))\nsucata.scene.spawn(game_manager)\n</code></pre> <p><code>config.lua</code> example \u2014 window setup, conventionally isolated in its own file:</p> <pre><code>sucata.window.set_window_size(960, 540)\nsucata.window.set_keep_aspect(1) -- 0 = off, 1 = keep aspect with bars, 2 = keep aspect with crop\nsucata.window.set_window_title(\"My Game\")\nsucata.window.set_vsync(1)\nsucata.window.show_debug_info(true) -- disable before shipping\nsucata.window.set_window_icon(\"src://icon.png\")\nsucata.window.set_fullscreen(false)\n</code></pre> <p>Asset/require paths use the <code>src://</code> virtual prefix, meaning \"project root\" \u2014 it resolves correctly both when running from loose files in dev mode and from inside the compiled <code>assets.scta</code> archive after <code>sucata build</code>. Always reference project assets this way: <code>\"src://sprites/ship.png\"</code>.</p>"},{"location":"agent-guide/#core-concepts","title":"Core concepts","text":""},{"location":"agent-guide/#scene","title":"Scene","text":"<p>A scene is just the pool of currently-active entities. <code>sucata.scene.load_scene(entities)</code> loads a whole array at once; <code>sucata.scene.spawn(entity)</code> adds one and returns its id.</p>"},{"location":"agent-guide/#entity","title":"Entity","text":"<p>An entity is a plain table with two fields:</p> <pre><code>{\n  state = { x = 0, y = 0 },      -- unique data for this entity (id is injected by the engine on spawn)\n  behaviours = { Behaviours.Foo, Behaviours.Bar }, -- ordered list of behaviours applied to it\n}\n</code></pre> <p>Entities are usually built via a factory function in <code>entities/&lt;name&gt;.lua</code>:</p> <pre><code>-- entities/player.lua\nlocal function player(x, y)\n  return {\n    state = { x = x, y = y },\n    behaviours = { Behaviours.Player, Behaviours.Shooter, Behaviours.DrawSprite },\n  }\nend\n\nreturn player\n</code></pre> <pre><code>sucata.scene.spawn(Player(480, 500))\n</code></pre>"},{"location":"agent-guide/#behaviour-the-core-building-block","title":"Behaviour \u2014 the core building block","text":"<p>A behaviour is a stateless, reusable table of up to four optional functions, all receiving the entity's <code>state</code> table:</p> <pre><code>return {\n  init = function(state) end,  -- once, when the entity is spawned/scene loads\n  tick = function(state) end,  -- every frame, before drawing \u2014 game logic, input, physics\n  draw = function(state) end,  -- every frame \u2014 call sucata.graphic.* here ONLY (see Rendering)\n  free = function(state) end,  -- once, right before the entity is removed from the scene\n}\n</code></pre> <p>Behaviours on an entity run in the order listed. Put logic before rendering (e.g. <code>Player</code> movement before <code>DrawSprite</code>).</p> <p>Critical performance/correctness rule: Sucata reuses behaviours that share the same Lua table pointer identity. Always define each behaviour once and expose it through a single shared table, conventionally <code>behaviours/init.lua</code>:</p> <p><code>lua -- behaviours/init.lua return {   DrawSprite = require(\"behaviours.draw_sprite\"),   Player     = require(\"behaviours.player\"),   Shooter    = require(\"behaviours.shooter\"), }</code> <code>lua -- main.lua Behaviours = require(\"behaviours\")</code> Then reference <code>Behaviours.Player</code> etc. everywhere \u2014 never <code>require(\"behaviours.player\")</code> again from inside entity files. Requiring it fresh in multiple places would defeat behaviour reuse.</p> <p>A common building-block behaviour worth copying into most projects, <code>DrawSprite</code> (renders <code>state.texture</code>/<code>width</code>/<code>height</code> if present):</p> <pre><code>return {\n  init = function(state)\n    state.x = state.x or 0\n    state.y = state.y or 0\n    state.width = state.width or 32\n    state.height = state.height or 32\n    state.texture = state.texture or \"\"\n    state.atlas_x = state.atlas_x or 0\n  end,\n  draw = function(state)\n    sucata.graphic.draw_rect({\n      x = state.x, y = state.y,\n      width = state.width, height = state.height,\n      texture = state.texture,\n      origin = 0.5, -- center the sprite on x,y instead of top-left\n      atlas_size = state.atlas_size,\n      atlas_x = state.atlas_x,\n    })\n  end\n}\n</code></pre> <p>Another common one, <code>ApplyForces</code> (simple velocity integration):</p> <pre><code>return {\n  init = function(state)\n    state.x = state.x or 0\n    state.y = state.y or 0\n    state.force_x = state.force_x or 0\n    state.force_y = state.force_y or 0\n  end,\n  tick = function(state)\n    local dt = sucata.time.get_delta()\n    state.x = state.x + state.force_x * dt\n    state.y = state.y + state.force_y * dt\n  end\n}\n</code></pre>"},{"location":"agent-guide/#no-hierarchy-relationships-via-ids","title":"No hierarchy \u2014 relationships via ids","text":"<p>Entities are flat; there's no parent/child tree. To relate entities, store one's id in another's state and resolve it later:</p> <pre><code>local bullet_id = sucata.scene.spawn(Bullet())\nself.child_id = bullet_id\n...\nlocal bullet = sucata.scene.find_by_id(self.child_id)\nif bullet then bullet.x = self.x end\n</code></pre>"},{"location":"agent-guide/#tags","title":"Tags","text":"<p>Group entities for querying (e.g. all enemies of a kind):</p> <pre><code>sucata.scene.add_tag(state, \"meteor\")\nlocal meteors = sucata.scene.get_entities_by_tag(\"meteor\")\nfor _, id in ipairs(meteors) do\n  local meteor = sucata.scene.find_by_id(id)\n  -- ...\nend\n</code></pre> <p>Also: <code>sucata.scene.has_tag</code>, <code>sucata.scene.remove_tag</code>.</p>"},{"location":"agent-guide/#globalpersistent-entities","title":"Global/persistent entities","text":"<p>For systems that should survive scene reloads (e.g. a \"game manager\" holding score across level transitions), use <code>sucata.scene.load_global(key, entity)</code> / <code>sucata.scene.get_global(key)</code> / <code>sucata.scene.unload_global(key)</code> instead of <code>spawn</code>.</p>"},{"location":"agent-guide/#events","title":"Events","text":"<p>Simple pub/sub, decoupling systems (e.g. a meteor tells the game manager it reached the ground without knowing the game manager exists):</p> <pre><code>-- emitter (e.g. inside a meteor's tick)\nsucata.events.emit(\"meteor_reached\", state)\n\n-- listener (e.g. inside the game manager's init)\nsucata.events.on(state, \"meteor_reached\", function(meteor_state)\n  -- react\nend)\n</code></pre>"},{"location":"agent-guide/#timers","title":"Timers","text":"<pre><code>sucata.time.create_timer(function()\n  sucata.scene.spawn(meteor())\nend, { time = 5, loop = true, auto_start = true })\n</code></pre> <p>Or the shorthand <code>sucata.time.create_timer(callback, seconds)</code> for a one-shot. Also: <code>sucata.time.pause_timer(id)</code>, <code>sucata.time.stop_timer(id)</code>, <code>sucata.time.get_delta()</code>, <code>sucata.time.get_fps()</code>, <code>sucata.time.get_time_scale()</code>/<code>set_time_scale()</code> (slow-mo / pause effects).</p>"},{"location":"agent-guide/#states-pattern-optional-but-recommended","title":"\"States\" pattern (optional but recommended)","text":"<p>Plain modules of pure functions that mutate a piece of state, shareable across multiple behaviours instead of duplicating logic:</p> <pre><code>-- states/health.lua\nlocal function remove(state)\n  if not state.health then return end\n  state.health = state.health - 1\nend\nreturn { remove = remove }\n</code></pre> <p>Used from any behaviour: <code>local health = require(\"states.health\"); health.remove(meteor)</code>.</p>"},{"location":"agent-guide/#rendering","title":"Rendering","text":"<ul> <li><code>sucata.graphic.draw_rect(props)</code> and <code>sucata.graphic.draw_text(props)</code> are the only two draw primitives, and they may only be called inside a behaviour's <code>draw(state)</code> function \u2014 calling them elsewhere (init/tick) has no effect since drawing happens in a dedicated render pass.</li> <li>The engine batches all draw calls into one render queue per frame, grouped by <code>(z_index, texture, fixed, shader)</code>. For performance: reuse the same texture across similar sprites (use a texture atlas + <code>atlas_x</code>/<code>atlas_y</code>/<code>atlas_size</code> frame selection instead of many separate textures), and minimize the number of distinct <code>draw_text</code> calls.</li> <li><code>origin = 0.5</code> centers the sprite on <code>x,y</code> (default origin is <code>0,0</code> = top-left corner).</li> <li><code>fixed = true</code> on <code>RectProps</code>/<code>TextProps</code> renders relative to the screen, ignoring the camera (for UI/HUD).</li> <li>Texture atlas animation pattern (change frame based on state): set <code>atlas_x</code>/<code>atlas_y</code> in <code>tick</code>, e.g. <code>state.atlas_x = state.health - 1</code>.</li> <li>Camera: <code>sucata.camera.get_camera_position/set_camera_position</code>, <code>get_camera_rotation/set_camera_rotation</code>, <code>get_camera_zoom/set_camera_zoom</code> \u2014 moves/rotates/zooms the (non-fixed) world view.</li> <li>Custom shaders: <code>sucata.graphic.load_shader(path)</code> (loads a compiled <code>.schd</code>) \u2192 pass the returned id as <code>shader</code> in <code>draw_rect</code>/<code>draw_text</code> props, or <code>sucata.graphic.add_post_processing(shader_id)</code> for full-screen post-fx, tunable at runtime with <code>sucata.graphic.set_post_processing_args(shader_id, field, value)</code>.</li> </ul>"},{"location":"agent-guide/#input","title":"Input","text":"<pre><code>if sucata.input.is_held(\"left\", \"a\") then ... end\nif sucata.input.is_pressed(\"space\", \"enter\") then ... end -- fires once, on the frame pressed\n</code></pre> <p><code>is_held</code>/<code>is_pressed</code>/<code>is_released</code> all take a variable number of key names (any match counts). Also: <code>sucata.input.get_mouse_position()</code>, <code>get_relative_mouse_position()</code> (camera-adjusted), <code>get_mouse_scroll()</code>, <code>get_key()</code>, and <code>sucata.input.is_hover({id, x, y, width, height, z_index?, fixed?})</code> for clickable UI regions (call every frame in <code>draw</code>; highest <code>z_index</code> wins on overlap).</p> <p>Gamepad mirrors the same shape: <code>sucata.gamepad.is_held/is_pressed/is_released(button, device?)</code>, <code>get_axis(axis, device?)</code>, <code>get_count()</code>, <code>get_info(device)</code>.</p>"},{"location":"agent-guide/#audio","title":"Audio","text":"<pre><code>sucata.audio.play({ sound = \"src://sounds/shoot.ogg\" })\nlocal music_id = sucata.audio.play({ sound = \"src://sounds/music.ogg\", loop = true, volume = 0.5 })\n</code></pre> <p>Returns a <code>sound_id</code> usable with <code>stop</code>/<code>pause</code>/<code>unpause</code>/<code>get_volume</code>/<code>set_volume</code>/<code>get_pitch</code>/<code>set_pitch</code>. Sounds can be mixed by <code>group</code> (default <code>\"default\"</code>), independently controllable via <code>get_group_volume</code>/<code>set_group_volume</code>/<code>get_group_pitch</code>/<code>set_group_pitch</code> (e.g. separate \"sfx\"/\"music\" groups).</p>"},{"location":"agent-guide/#filesystem","title":"Filesystem","text":"<p><code>sucata.filesystem.*</code> (<code>exists</code>, <code>read_file</code>, <code>write</code>, <code>mkdir</code>, <code>remove</code>, <code>rename</code>, <code>read_dir</code>) work with the same virtual prefixes: <code>src://</code> (project root \u2014 read-only once built), <code>data://</code> (per-user writable directory \u2014 use this for save files/settings), <code>build://</code> (build output dir).</p>"},{"location":"agent-guide/#building","title":"Building","text":"<pre><code>sucata build main.lua --icon src://icon.png\n</code></pre> <p>Under the hood: <code>sucata</code> (the CLI) walks your Lua <code>require</code>s and <code>src://</code> asset references, compiles the Lua to bytecode, LZ4-compresses everything into a single <code>assets.scta</code> archive, then copies the <code>sucata-player</code> runtime binary (+ needed DLLs) and appends a signature binding it to that exact <code>assets.scta</code>. The resulting executable loads its bundled assets on launch \u2014 no separate installer needed. Currently you can only build for the OS you're running on (no cross-compilation yet).</p>"},{"location":"agent-guide/#full-lua-api-condensed-reference","title":"Full Lua API \u2014 condensed reference","text":"<p>Every module below is <code>sucata.&lt;module&gt;</code>. Full prose docs (with all fields/defaults) are under References in the sidebar.</p> <p><code>sucata.scene</code> \u2014 <code>load_scene(entities)</code>, <code>spawn(entity) -&gt; id</code>, <code>spawns(entities) -&gt; ids</code>, <code>find_by_id(id) -&gt; entity|nil</code>, <code>destroy(entity_or_id) -&gt; bool</code>, <code>destroys(entities) -&gt; undestroyed_ids</code>, <code>add_tag/has_tag/remove_tag(entity_or_id, tag)</code>, <code>get_entities() -&gt; ids</code>, <code>get_entities_by_tag(tag) -&gt; ids</code>, <code>clear_entities()</code>, <code>init(callback)</code> (run once scene starts), <code>load_global(key, entity) -&gt; id</code>, <code>get_global(key) -&gt; entity|nil</code>, <code>unload_global(key)</code>. Full reference</p> <p><code>sucata.graphic</code> \u2014 <code>draw_rect(RectProps)</code>, <code>draw_text(TextProps)</code> (draw-phase only), <code>set_background_color(hex)</code>, <code>load_shader(path) -&gt; id|nil</code>, <code>add_post_processing(shader_id)</code>, <code>set_post_processing_args(shader_id, field, number|string|table)</code>, <code>remove_post_processing(shader_id)</code>. <code>RectProps</code>: <code>x,y,width,height,color,z_index,texture,scale(_x/_y),fixed,tiled,tile_size,tile_width,tile_height,origin(_x/_y),rotation,opacity,atlas_size,atlas_width,atlas_height,atlas_spacing,atlas_margin,atlas_x,atlas_y,shader,shader_args</code>. <code>TextProps</code>: <code>x,y,text,size,font,color,z_index,scale(_x/_y),fixed,origin(_x/_y),rotation,opacity,align,max_width,shader,shader_args</code>. Full reference</p> <p><code>sucata.input</code> \u2014 <code>is_pressed/is_held/is_released(...keys) -&gt; bool</code>, <code>get_mouse_position()/get_relative_mouse_position() -&gt; x,y</code>, <code>get_mouse_scroll() -&gt; x,y</code>, <code>get_key() -&gt; code</code>, <code>is_hover({id,x,y,width,height,z_index?,fixed?}) -&gt; bool</code>. <code>Key</code> values: <code>\"mouse_left/right/middle\"</code>, <code>\"a\"</code>\u2013<code>\"z\"</code>, <code>\"space\"</code>/<code>\" \"</code>, <code>\"escape\"</code>/<code>\"esc\"</code>, <code>\"enter\"</code>/<code>\"return\"</code>, <code>\"shift\"</code>, <code>\"ctrl\"</code>/<code>\"control\"</code>, <code>\"alt\"</code>, <code>\"apostrophe\"</code>, <code>\"tab\"</code>, <code>\"up\"/\"down\"/\"left\"/\"right\"</code>. Full reference</p> <p><code>sucata.gamepad</code> \u2014 <code>get_count() -&gt; n</code>, <code>get_info(device) -&gt; name</code>, <code>get_axis(Axis, device?) -&gt; value, device_used</code>, <code>is_held/is_pressed/is_released(Button, device?) -&gt; bool, device_used</code>. <code>Axis</code>: <code>left_x/left_y/right_x/right_y/trigger_left/trigger_right</code>. <code>Button</code>: <code>a/b/x/y/back/guide/start/left_stick/right_stick/left_shoulder/right_shoulder/dpad_up/down/left/right</code>. Full reference</p> <p><code>sucata.audio</code> \u2014 <code>play({sound,volume?,pitch?,group?,loop?}) -&gt; sound_id</code>, <code>stop/pause/unpause(sound_id)</code>, <code>get_volume/set_volume(sound_id[, v])</code>, <code>get_pitch/set_pitch(sound_id[, p])</code>, <code>get_group_volume/set_group_volume(group_id[, v])</code>, <code>get_group_pitch/set_group_pitch(group_id[, p])</code>. Full reference</p> <p><code>sucata.time</code> \u2014 <code>get_delta() -&gt; seconds</code>, <code>get_fps() -&gt; n</code>, <code>create_timer(callback, seconds|Timer) -&gt; id</code>, <code>pause_timer(id)</code>, <code>stop_timer(id)</code>, <code>get_time_scale()/set_time_scale(scale)</code>. <code>Timer</code>: <code>{time, auto_start=true, one_shot=true, loop=false}</code>. Full reference</p> <p><code>sucata.events</code> \u2014 <code>emit(name, data)</code>, <code>on(owner, name, callback)</code>. Full reference</p> <p><code>sucata.camera</code> \u2014 <code>get/set_camera_position(x,y)</code>, <code>get/set_camera_rotation(radians)</code>, <code>get/set_camera_zoom(zoom)</code>. Full reference</p> <p><code>sucata.window</code> \u2014 <code>get/set_mouse_lock(bool)</code>, <code>get/set_mouse_visible(bool)</code>, <code>get/set_window_title(str)</code>, <code>get/set_window_size(w,h)</code>, <code>get/set_fullscreen(bool)</code>, <code>get/set_vsync(n)</code>, <code>quit()</code>, <code>show_debug_info(bool)</code>, <code>get/set_keep_aspect(0|1|2)</code>, <code>get/set_window_icon(path)</code>, <code>get/set_cursor(name)</code>, <code>on_init(callback)</code>, <code>on_exit(callback)</code>. Full reference</p> <p><code>sucata.filesystem</code> \u2014 <code>exists(path) -&gt; bool</code>, <code>remove(path)</code>, <code>mkdir(path)</code>, <code>read_file(path) -&gt; str|nil</code>, <code>read_dir(path) -&gt; table|nil</code>, <code>write(path, content) -&gt; bool</code>, <code>rename(old, new)</code>. Full reference</p> <p><code>sucata.math</code> \u2014 <code>clamp(v,min,max)</code>, <code>distance(p1,p2)</code>, <code>lerp(a,b,t)</code>, <code>overlapping(Rect,Rect) -&gt; bool, intersection|nil</code>, <code>screen_relative(ScreenRelativeRect) -&gt; x,y,w,h</code>, <code>smooth_index(current_time, interval, max_index) -&gt; index</code>, <code>normalize(...) -&gt; ...</code>, <code>move_towards(current, target, step)</code>. Full reference</p> <p><code>sucata.meta</code> \u2014 <code>OS</code> (string), <code>VERSION</code> (string). Full reference</p>"},{"location":"agent-guide/#gotchas-checklist","title":"Gotchas checklist","text":"<ul> <li>Only call <code>sucata.graphic.draw_*</code> from inside <code>draw(state)</code>.</li> <li>Always route behaviours through one shared table (<code>Behaviours = require(\"behaviours\")</code>), never <code>require</code> a behaviour module fresh in multiple files.</li> <li>Behaviours execute in the order listed on the entity \u2014 order logic before rendering.</li> <li><code>init</code>/<code>tick</code>/<code>draw</code>/<code>free</code> are all optional \u2014 omit what you don't need.</li> <li>Use <code>state.x = state.x or default</code> in <code>init</code> to make fields optional with sane defaults when constructing entities.</li> <li>Prefer one texture/atlas over many separate images, and batch text draws, for render performance.</li> <li>Use <code>src://</code> for bundled read-only assets, <code>data://</code> for writable save data.</li> <li>Disable <code>sucata.window.show_debug_info</code> before shipping.</li> </ul>"},{"location":"agent-guide/#related","title":"Related","text":"<ul> <li>First Project tutorial \u2014 the full step-by-step Asteroids-style walkthrough this guide is distilled from.</li> <li>CLI reference</li> <li>API references</li> <li>meteors-sucata \u2014 complete example game source.</li> </ul>"},{"location":"cli/","title":"Sucata CLI","text":"<p>This CLI of the Sucata, is a collection of helpers</p>"},{"location":"cli/#sucata-run","title":"sucata run","text":"<p>Runs a Sucata Lua script file</p> <p>arguments</p> <ul> <li> - It is the path of the main lua file  <li>--entity  (optional) - It is the lua file of a entity (for testing)"},{"location":"cli/#sucata-build","title":"sucata build","text":"<p>Builds Sucata into a binary game for the current OS</p> <p>arguments</p> <ul> <li> - It is the path of the main lua file  <li>--icon  (optional) - It is the icon for your game <p>For while, this is a Sucata limitation, you can only build the game for the current OS.</p>"},{"location":"cli/#sucata-version","title":"sucata version","text":"<p>Show the Sucata game engine version and release date</p>"},{"location":"cli/#sucata-shader","title":"sucata shader","text":"<p>Util to shaders on sucata</p>"},{"location":"cli/#sucata-shader-build","title":"sucata shader build","text":"<p>Builds a .glsl file to sucata shader file</p>"},{"location":"cli/#arguments","title":"arguments","text":"<ul> <li> - It is the path of the .glsl shader you wants do build"},{"location":"cli/#sucata-shader-create","title":"sucata shader create","text":"<p>Create a base .glsl shader file</p>"},{"location":"cli/#arguments_1","title":"arguments","text":"<ul> <li> - It is the path for create a .glsl from template <li>--post-procesing (optional) - Its a flag to create a template for post-processing [WIP]</li> <li>--font (optional) - Its a flag to create a template for font shader</li>"},{"location":"examples/","title":"Example Games","text":"<p>Example games created using Sucata</p>"},{"location":"examples/#meteors","title":"Meteors","text":"<p>A game inspired by the classic Asteroids game, where you need to survive as long as possible avoiding meteors.</p> <p>Repo</p>"},{"location":"third-party/","title":"Third Party","text":"<p>Technologies used on Sucata.</p>"},{"location":"third-party/#odin","title":"Odin","text":"<p>Programming language used to build the engine</p> <pre><code>Copyright (c) 2016-2025 Ginger Bill.\n\nLicensed under the zLib License.\n</code></pre> <p>odin-lang.org</p>"},{"location":"third-party/#lua","title":"lua","text":"<p>Scripting language used in the engine</p> <pre><code>Copyright (C) 1994-2024 Lua.org, PUC-Rio.\n\nLicensed under the MIT License.\n</code></pre> <p>lua.org</p>"},{"location":"third-party/#sokol","title":"sokol","text":"<p>Cross-platform development libraries for graphics, audio, and input handling</p> <pre><code>Copyright (c) 2019 Andre Weissflog\n\nLicensed under the MIT License.\n</code></pre> <p>floooh/sokol</p>"},{"location":"third-party/#sokol-tools","title":"sokol-tools","text":"<p>Cross-platform shader tools for building shader</p> <pre><code>Copyright (c) 2019 Andre Weissflog\n\nLicensed under the MIT License.\n</code></pre> <p>floooh/sokol-tools</p>"},{"location":"third-party/#miniaudio","title":"miniaudio","text":"<p>Single file audio playback and capture library</p> <pre><code>Copyright (c) 2025 David Reid\n\nLicensed under the MIT License.\n</code></pre> <p>mackron/miniaudio</p>"},{"location":"third-party/#lz4","title":"lz4","text":"<p>Fast compression algorithm for asset compression</p> <pre><code>This product includes LZ4, licensed under the BSD 2-Clause License.\n\nCopyright (c) Yann Collet\n</code></pre> <p>lz4/lz4</p>"},{"location":"third-party/#sdl","title":"SDL","text":"<p>Simple DirectMedia Layer for gamepad support</p> <pre><code>Copyright (c) 1997-2026 Sam Lantinga &lt;slouken@libsdl.org&gt;\n\nLicensed under the zLib License.\n</code></pre> <p>libsdl-org/SDL</p>"},{"location":"blog/","title":"Blog","text":""},{"location":"getting-started/concepts/","title":"Concepts","text":"<p>Sucata is a functional and modular game engine.</p>"},{"location":"getting-started/concepts/#how-the-engine-works","title":"How the engine works?","text":"<p>The engine interprets the lua source code, registering actions, calling sokol for graphic render, miniaudio for play audio, receiving gamepad input from sdl, keyboard and mouse inputs from sokol, and any events call back some lua functions </p>"},{"location":"getting-started/concepts/#scene","title":"Scene","text":"<p>A scene is a pool of entities, and it's the main way to organize your game, you can active a scene to load its entities.</p>"},{"location":"getting-started/concepts/#entity","title":"Entity","text":"<p>An entity is a table that contains state and behaviours, state is unique for the entity, behaviours it is scripts can be reused</p>"},{"location":"getting-started/concepts/#state","title":"State","text":"<p>The entity state contains all unique data about this entity, including your unique id</p>"},{"location":"getting-started/concepts/#id","title":"id","text":"<p>The unique id of the entity, generated by the engine when the entity is spawned.</p>"},{"location":"getting-started/concepts/#behaviours","title":"Behaviours","text":"<p>You can define a list of behaviours that will be executed to changed state</p> <p>Behaviours will be executing by the order defined</p>"},{"location":"getting-started/concepts/#behaviour","title":"Behaviour","text":"<p>Behaviour is a table with a collections of stateless functions, that recieve an state and changes it, the design of the behaviour it is to be modular, small scripts that can be reused in many entities</p>"},{"location":"getting-started/concepts/#initstate","title":"init(state)","text":"<p>Called when the scene is loaded</p> <p>This function will be called only once.</p> <p>This function is optional, and you can omit it if you don't need execute any code when the entity is loaded.</p>"},{"location":"getting-started/concepts/#tickstate","title":"tick(state)","text":"<p>Called every frame to update the entity</p> <p>You can call <code>sucata.time.get_delta()</code> to get the time since the last frame, and use it to update the entity's data.</p> <p>This function is optional, and you can omit it if you don't need execute any code every frame that the entity is loaded.</p>"},{"location":"getting-started/concepts/#drawstate","title":"draw(state)","text":"<p>Called every frame to draw the entity</p> <p>Normally used to draw graphics for the entity, using <code>sucata.graphics</code> functions.</p> <p>This function is optional, and you can omit it if you don't need to draw any graphics for the entity.</p>"},{"location":"getting-started/concepts/#freestate","title":"free(state)","text":"<p>Called when the scene is unloaded</p> <p>This function will be called only once, right before the entity is removed from the scene.</p> <p>This function is optional, and you can omit it if you don't need to execute any command before the entity is removed from the scene.</p>"},{"location":"getting-started/concepts/#rendering","title":"Rendering","text":""},{"location":"getting-started/concepts/#render-functions","title":"Render functions","text":"<p>Sucata have two functions to draw graphics:  - <code>sucata.graphic.rect({})</code> - Used to render rectangules, it can have textures - <code>sucata.graphic.text({})</code> - Used to render texts using a font, or default font from the system</p> <p>Render functions can only be called on draw() function from a behaviour</p>"},{"location":"getting-started/concepts/#render-logic","title":"Render logic","text":"<p>Every frame sucata has a renderQueue, that has all request graphic render</p> <p>For performatic reasons sucata tries to render all together, and to this work, sucata groups by draw call all graphic that have the same z_index, texture, fixed and shader.</p> <p>Recommendation, always try to make draws with the same texture, so creates a tilemap and use atlas from sucata.graphic.draw_rect()</p> <p>And for text, sucata draws each character in the same draw call.</p> <p>Recommentation, always try to make all text in less sucata.graphic.draw_text calls you can</p>"},{"location":"getting-started/concepts/#relationships","title":"Relationships","text":"<p>The engine don't have hirarchy of entities, instead all entities have relationships with each other, and you can use the unique id of the entities to reference them, and create relationships between them.</p> <p>For example:</p> <pre><code>local children_id = sucata.scene.spawn(Bullet())\nself.children = children_id\n</code></pre> <p>In this example, we are spawning a new entity of type <code>Bullet</code> and storing its unique id in the <code>children</code> field of the current entity, so we can reference it later.</p> <pre><code>local bullet_entity = sucata.scene.find_by_id(self.children)\nbullet_entity.x = self.x\nbullet_entity.y = self.y\n</code></pre>"},{"location":"getting-started/concepts/#building","title":"Building","text":"<p>To build your game in sucata you can run <code>sucata build .</code> and build the game for the operational system you are using</p> <p>For now, while has a limitation that you can't cross build, but we plan to make cross build in the future</p>"},{"location":"getting-started/concepts/#how-build-works","title":"How build works?","text":"<p>In the sucata files, we have the <code>sucata</code> and <code>sucata-player</code>, <code>sucata</code> is the cli helper, that has all commands, and <code>sucata-player</code> is the executable that interprets the code and run, when you build the game, sucata copys the <code>sucata-player</code>, all DLLs and bundle all assets that you used in your source code into the <code>assets.scta</code>, after all that, sucata signs the copied <code>sucata-player</code>, to only run with the specified <code>assets.scta</code>, when you open the executable, will load the <code>assets.scta</code> and runs the game.</p>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>This guide explains how to install sucata on Windows, macOS, and Linux, or build it from source.</p>"},{"location":"getting-started/installation/#installing-prebuilt-binaries","title":"\ud83d\udce6 Installing Prebuilt Binaries","text":"<p>Install sucata with a prebuilt binary, without need to configure or install any dependencies.</p>"},{"location":"getting-started/installation/#windows","title":"\ud83e\ude9f Windows","text":"<ol> <li>Open PowerShell.</li> <li>Run the following command:</li> </ol> <pre><code>irm https://github.com/sucata-engine/sucata/raw/branch/main/install_windows.ps1 | iex\n</code></pre> <ol> <li>Restart your terminal.</li> <li>Now you can use the command <code>sucata</code> in your terminal.</li> </ol> <p>Warning This command downloads and executes the official installation script. It will: Download sucata, Install it on your system and Add it to your system PATH</p>"},{"location":"getting-started/installation/#macoslinux","title":"\ud83d\udc27 MacOs/Linux","text":"<ol> <li>Open your terminal.</li> <li>Run the following command:</li> </ol> <pre><code>curl -fsSL https://github.com/sucata-engine/sucata/raw/branch/main/install_unix.sh | bash\n</code></pre> <ol> <li>Restart your terminal.</li> <li>Now you can use the command <code>sucata</code> in your terminal.</li> </ol> <p>Warning This command downloads and executes the official installation script. It will: Download sucata, Install it on your system and Add it to your system PATH</p>"},{"location":"getting-started/installation/#building-from-source","title":"\ud83d\udee0 Building from Source","text":"<p>If you prefer to build sucata manually:</p> <ol> <li>You need to install: Odin and C++ compiler (MSVC, Clang, or GCC)</li> <li>Clone the repositories: sucata-player and sucata-cli</li> <li>Go to the folder /sokol/ and    build the sokol binaries</li> <li>You go to the repository root, then build the sucata binaries using:</li> </ol> <pre><code>odin build .\n</code></pre> <ol> <li>(optional) Add the binary to the path of your system</li> <li>All done you now can use the sucata</li> </ol>"},{"location":"getting-started/installation/#installing-lua-addon","title":"\ud83c\udf19 Installing Lua Addon","text":"<p>You can install an sucata lua addon on the sumneko's lua extension for better auto completion</p> <ol> <li>Install sumneko's lua extension in your favorite    IDE</li> <li>Open the Addon Manager</li> <li>Search for 'sucata' and install the sucata addon</li> </ol>"},{"location":"getting-started/first-project/adding-textures/","title":"Adding Textures","text":"<p>In this section we will add textures to our game. We will add textures for the player, meteors, and bullets.</p> <p>You can download the assets used in this tutorial from here.</p>"},{"location":"getting-started/first-project/adding-textures/#bullet-texture","title":"Bullet Texture","text":"<p>Let's start with the simplest texture: the bullet.</p> <p>Update the state in <code>entities/bullet.lua</code>:</p> <pre><code>local function bullet(x, y)\n    return {\n        state = {\n            x = x,\n            y = y,\n            texture = \"src://sprites/bullet.png\", -- Texture path\n            width = 16, -- Bullet width\n            height = 16 -- Bullet height\n        },\n        behaviours = {\n            Behaviours.Bullet,\n            Behaviours.ApplyForces,\n            Behaviours.DrawSprite,\n        }\n    }\nend\n\nreturn bullet\n</code></pre> <p>Note <code>src://</code> represents the root directory of the project. Use this prefix whenever referencing files inside the project.</p> <p>The <code>DrawSprite</code> behaviour automatically renders textures when the <code>texture</code>, <code>width</code>, and <code>height</code> fields are present in the entity state.</p> <p>The result should look like this:</p> <p></p>"},{"location":"getting-started/first-project/adding-textures/#meteor-texture","title":"Meteor Texture","text":"<p>The meteor texture uses a texture atlas so the meteor appearance changes based on its health.</p> <p>First, define the texture in the meteor entity (<code>entities/meteor.lua</code>):</p> <pre><code>local function meteor()\n    return {\n        state = {\n            y = -16,\n            texture = \"src://sprites/meteor.png\", -- Meteor texture\n            atlas_size = 8 -- Split the texture into 8 horizontal frames\n        },\n        behaviours = {\n            Behaviours.RandomStartPosition,\n            Behaviours.Meteor,\n            Behaviours.ApplyForces,\n            Behaviours.DrawSprite,\n        }\n    }\nend\n\nreturn meteor\n</code></pre> <p>Now update the meteor behaviour in <code>behaviours/meteor.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        sucata.scene.add_tag(state, \"meteor\")\n\n        state.speed = state.speed or math.random(100, 200)\n        state.health = state.health or math.random(1, 5)\n        state.force_y = state.speed\n    end,\n\n    tick = function(state)\n        if state.y &gt; 540 then\n            sucata.events.emit(\"meteor_reached\", state)\n            sucata.scene.destroy(state)\n        end\n\n        -- Select the texture frame based on meteor health\n        state.atlas_x = state.health - 1\n    end\n}\n</code></pre> <p>Now the meteor sprite will change depending on its health:</p> <p></p>"},{"location":"getting-started/first-project/adding-textures/#player-texture","title":"Player Texture","text":"<p>For the player we will add a texture atlas that represents the ship inclination.</p> <p>First create a new behaviour in <code>behaviours/inclination.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        state.inclination = 2 -- Initial inclination frame\n    end,\n\n    tick = function(state)\n        local dt = sucata.time.get_delta()\n\n        if sucata.input.is_held(\"left\", \"a\") then\n            state.inclination = sucata.math.clamp(\n                state.inclination - (15 * dt),\n                0,\n                4\n            )\n\n        elseif sucata.input.is_held(\"right\", \"d\") then\n            state.inclination = sucata.math.clamp(\n                state.inclination + (15 * dt),\n                0,\n                4\n            )\n\n        else\n            state.inclination = sucata.math.lerp(\n                state.inclination,\n                2,\n                dt * 10\n            )\n        end\n\n        state.atlas_x = math.floor(state.inclination)\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    Inclination = require(\"behaviours.inclination\"),\n}\n</code></pre> <p>Now update the player entity in <code>entities/player.lua</code>:</p> <pre><code>local function player(x, y)\n    return {\n        state = {\n            x = x,\n            y = y,\n            texture = \"src://sprites/ship.png\", -- Player texture\n            atlas_size = 8 -- Split texture into 8 frames\n        },\n        behaviours = {\n            Behaviours.Player,\n            Behaviours.Inclination,\n            Behaviours.Shooter,\n            Behaviours.DrawSprite,\n        }\n    }\nend\n\nreturn player\n</code></pre> <p>Now the player ship will tilt depending on movement:</p> <p></p>"},{"location":"getting-started/first-project/configuring-project/","title":"Starting the Project","text":""},{"location":"getting-started/first-project/configuring-project/#getting-started","title":"Getting Started","text":"<p>First, make sure that the Sucata Engine is installed on your system. You can follow the installation guide on the installation page.</p> <p>To verify that Sucata is installed correctly, run the following command in your terminal:</p> <pre><code>sucata version\n</code></pre> <p>If the installation was successful, the engine version will be printed in the terminal.</p> <p>Next, create a new folder for your project. Inside this folder, create a file called <code>main.lua</code>. This file will be the main entry point of your game.</p>"},{"location":"getting-started/first-project/configuring-project/#configuration","title":"Configuration","text":"<p>To configure the game window, you can use functions from <code>sucata.window</code>.</p> <p>It is recommended to place these settings in a separate file called <code>config.lua</code>.</p> <pre><code>sucata.window.set_window_size(960, 540) -- Game window size in pixels\nsucata.window.set_keep_aspect(1) -- Maintain aspect ratio when resizing the window\n-- (0 = off, 1 = keep aspect with bars, 2 = keep aspect with crop)\n\nsucata.window.set_window_title(\"My First Game\") -- Window title\nsucata.window.set_vsync(1) -- Enable VSync (0 = off, 1 = on, higher values for specific intervals)\n\nsucata.window.show_debug_info(true) -- Display debug information such as FPS and engine stats\nsucata.window.set_window_icon(\"src://icon.png\") -- Window icon\nsucata.window.set_fullscreen(false) -- Enable or disable fullscreen mode\n</code></pre> <p>Note Due to limitations of sokol, VSync will always remain enabled.</p> <p>Tip Remember to disable <code>sucata.window.show_debug_info()</code> when releasing your game.</p> <p>Finally, load the configuration file inside <code>main.lua</code>:</p> <pre><code>require(\"config\")\n</code></pre>"},{"location":"getting-started/first-project/creating-meteors/","title":"Creating Meteors","text":"<p>Now we will create the enemies of the game: meteors that fall from the sky.</p>"},{"location":"getting-started/first-project/creating-meteors/#creating-the-meteor-behaviour","title":"Creating the Meteor Behaviour","text":"<p>First, we will create a behaviour responsible for making meteors fall.</p> <p>Create the file <code>behaviours/apply_forces.lua</code>, that will make the meteor move:</p> <pre><code>return {\n    init = function (state)\n        state.x = state.x or 0\n        state.y = state.y or 0\n        state.force_x = state.force_x or 0\n        state.force_y = state.force_y or 0\n    end,\n    tick = function (state) \n        local dt = sucata.time.get_delta()\n        state.x = state.x + (state.force_x * dt) -- Move x by force_x in the state\n        state.y = state.y + (state.force_y * dt) -- Move y by force_y in the state\n    end\n}\n</code></pre> <p>Create the file <code>behaviours/meteor.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        sucata.scene.add_tag(state, \"meteor\") -- Add the \"meteor\" tag to the entity\n        state.speed = state.speed or math.random(100, 200) -- Random fall speed\n        state.health = state.health or math.random(1, 5) -- Random meteor health\n        state.force_y = state.speed -- define force to move\n    end,\n\n    tick = function(state)\n        if state.y &gt; 540 then -- When the meteor leaves the screen\n            sucata.events.emit(\"meteor_reached\", state) -- Emit an event\n            sucata.scene.destroy(state) -- Destroy the meteor\n        end\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    ApplyForces = require(\"behaviours.apply_forces\"),\n    Meteor = require(\"behaviours.meteor\"),\n}\n</code></pre>"},{"location":"getting-started/first-project/creating-meteors/#creating-the-meteor-entity","title":"Creating the Meteor Entity","text":"<p>Now we will create the meteor entity.</p> <p>Create the file <code>entities/meteor.lua</code>:</p> <pre><code>local function meteor(x, y)\n    return {\n        state = {\n            x = x, -- Meteor position\n            y = y\n        },\n\n        behaviours = {\n            Behaviours.Meteor, -- Meteor logic\n            Behaviours.ApplyForces, -- Apply forces logic\n            Behaviours.DrawSprite -- Render the meteor\n        }\n    }\nend\n\nreturn meteor\n</code></pre> <p>Spawn a meteor in <code>main.lua</code>:</p> <pre><code>local Meteor = require(\"entities.meteor\")\n\nsucata.scene.spawn(Meteor(300, 100))\n</code></pre> <p>The game should now look like this:</p> <p></p>"},{"location":"getting-started/first-project/creating-meteors/#creating-a-random-start-position-behaviour","title":"Creating a Random Start Position Behaviour","text":"<p>Now we will create a behaviour that spawns entities at a random position.</p> <p>Create the file <code>behaviours/random_start_position.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        state.x = state.x or math.random(16, 960 - 16) -- Random X within screen bounds\n        state.y = state.y or math.random(16, 540 - 16) -- Random Y within screen bounds\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    RandomStartPosition = require(\"behaviours.random_start_position\")\n}\n</code></pre>"},{"location":"getting-started/first-project/creating-meteors/#spawning-meteors-at-random-positions","title":"Spawning Meteors at Random Positions","text":"<p>Now we will update the meteor entity so it spawns at a random X position.</p> <p>Update <code>entities/meteor.lua</code>:</p> <pre><code>local function meteor()\n    return {\n        state = {\n            y = -16 -- Start slightly above the screen\n        },\n\n        behaviours = {\n            Behaviours.RandomStartPosition, -- Random spawn position\n            Behaviours.Meteor, -- Meteor logic\n            Behaviours.ApplyForces, -- Apply forces logic\n            Behaviours.DrawSprite -- Render the meteor\n        }\n    }\nend\n\nreturn meteor\n</code></pre> <p>Now meteors will spawn with a random X position and fall from the top of the screen.</p>"},{"location":"getting-started/first-project/creating-player/","title":"Creating the Player","text":"<p>In this section, we will create the first entity of the game: the player. We will also add movement so the player can move around the screen.</p>"},{"location":"getting-started/first-project/creating-player/#creating-the-draw-behaviour","title":"Creating the Draw Behaviour","text":"<p>First, we need a behaviour responsible for rendering a sprite.</p> <p>Create the file <code>behaviours/draw_sprite.lua</code>:</p> <pre><code>return {\n    init = function(state) -- Called once when the entity enters the scene\n        -- Try to get values from the state, fallback to defaults if missing\n        state.x = state.x or 0\n        state.y = state.y or 0\n        state.width = state.width or 32\n        state.height = state.height or 32\n        state.texture = state.texture or \"\"\n        state.atlas_x = state.atlas_x or 0\n    end,\n\n    draw = function(state) -- Called every frame during rendering\n        sucata.graphic.draw_rect({\n            x = state.x,\n            y = state.y,\n            width = state.width,\n            height = state.height,\n            texture = state.texture,\n            origin = 0.5,\n            atlas_size = state.atlas_size,\n            atlas_x = state.atlas_x\n        })\n    end\n}\n</code></pre> <p>Now we need to register this behaviour.</p> <p>Create the file <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    DrawSprite = require(\"behaviours.draw_sprite\")\n}\n</code></pre> <p>Then expose the behaviours globally in <code>main.lua</code>:</p> <pre><code>Behaviours = require(\"behaviours\")\n</code></pre> <p>Tip It is recommended to create a single shared reference for behaviours instead of requiring them locally. Sucata reuses behaviours that share the same pointer, which improves performance.</p>"},{"location":"getting-started/first-project/creating-player/#creating-the-player-entity","title":"Creating the Player Entity","text":"<p>Now we will create the player entity.</p> <p>Create the file <code>entities/player.lua</code>:</p> <pre><code>local function player(x, y)\n    return {\n        state = {\n            x = x, -- Player position\n            y = y\n        },\n\n        behaviours = {\n            Behaviours.DrawSprite -- Render the player\n        }\n    }\nend\n\nreturn player\n</code></pre> <p>Now spawn the player in <code>main.lua</code>:</p> <pre><code>local Player = require(\"entities.player\")\n\nsucata.scene.spawn(Player(480, 500)) -- Spawn player near the center of the screen\n</code></pre> <p>The game should now look like this:</p> <p></p>"},{"location":"getting-started/first-project/creating-player/#creating-the-player-behaviour","title":"Creating the Player Behaviour","text":"<p>Next, we will add player movement using the keyboard.</p> <p>Create the file <code>behaviours/player.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        state.speed = state.speed or 200 -- Default movement speed\n    end,\n\n    tick = function(state)\n        local dt = sucata.time.get_delta() -- Time between frames\n\n        if sucata.input.is_held(\"left\", \"a\") then\n            state.x = state.x - state.speed * dt\n        elseif sucata.input.is_held(\"right\", \"d\") then\n            state.x = state.x + state.speed * dt\n        end\n    end\n}\n</code></pre> <p>Player input is handled through the <code>sucata.input</code> module.</p> <p>Now register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    DrawSprite = require(\"behaviours.draw_sprite\"),\n    Player = require(\"behaviours.player\")\n}\n</code></pre> <p>Then add the player behaviour to the entity in <code>entities/player.lua</code>:</p> <pre><code>local function player(x, y)\n    return {\n        state = {\n            x = x,\n            y = y\n        },\n\n        behaviours = {\n            Behaviours.Player,     -- Handle player logic\n            Behaviours.DrawSprite  -- Render player\n        }\n    }\nend\n\nreturn player\n</code></pre> <p>Note Behaviours are executed in order, so the player logic runs before rendering.</p> <p>Now the player can move:</p> <p></p>"},{"location":"getting-started/first-project/creating-player/#adding-screen-boundaries","title":"Adding Screen Boundaries","text":"<p>To finish the initial player implementation, we will prevent the player from leaving the screen.</p> <p>Update <code>behaviours/player.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        state.speed = state.speed or 200\n    end,\n\n    tick = function(state)\n        local dt = sucata.time.get_delta()\n\n        -- Add horizontal boundaries\n        if sucata.input.is_held(\"left\", \"a\") and state.x &gt; 20 then\n            state.x = state.x - state.speed * dt\n        elseif sucata.input.is_held(\"right\", \"d\") and state.x &lt; 492 then\n            state.x = state.x + state.speed * dt\n        end\n    end\n}\n</code></pre> <p>Now we have the first working version of the player!</p>"},{"location":"getting-started/first-project/game-manager/","title":"Game Manager","text":"<p>Now we will create a Game Manager, responsible for managing core game systems such as:</p> <ul> <li>Meteor spawning</li> <li>Player health</li> <li>Player points</li> </ul>"},{"location":"getting-started/first-project/game-manager/#meteor-spawner","title":"Meteor Spawner","text":"<p>First, we will create a system that spawns a new meteor every 5 seconds.</p> <p>Create the file <code>behaviours/meteor_spawner.lua</code>:</p> <pre><code>local meteor = require(\"entities.meteor\")\n\nreturn {\n    init = function(state)\n        state.spawner_time = state.spawner_time or 5 -- Time between meteor spawns\n\n        -- Create a timer that spawns meteors\n        sucata.time.create_timer(function ()\n            sucata.scene.spawn(meteor())\n        end, {\n            time = state.spawner_time,\n            loop = true,\n            auto_start = true\n        })\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    MeteorSpawner = require(\"behaviours.meteor_spawner\")\n}\n</code></pre>"},{"location":"getting-started/first-project/game-manager/#game-manager-entity","title":"Game Manager Entity","text":"<p>Now we will create the Game Manager entity.</p> <p>Create the file <code>entities/game_manager.lua</code>:</p> <pre><code>return {\n    state = {},\n    behaviours = {\n        Behaviours.MeteorSpawner\n    }\n}\n</code></pre> <p>Spawn the Game Manager in <code>main.lua</code>:</p> <pre><code>local game_manager = require(\"entities.game_manager\")\n\nsucata.scene.spawn(game_manager)\n</code></pre>"},{"location":"getting-started/first-project/game-manager/#player-health","title":"Player health","text":"<p>Now we will implement the player's health system.</p> <p>First, create a state module in <code>states/player_health.lua</code>:</p> <pre><code>local function remove(state)\n    if not state.health then -- Safe state modification\n        return\n    end\n\n    state.health = state.health - 1\nend\n\nreturn {\n    remove = remove\n}\n</code></pre> <p>Note States are modules that contain logic for modifying entity state. They can be called from any behaviour.</p> <p>Now create the behaviour <code>behaviours/player_health.lua</code>:</p> <pre><code>local health = require(\"states.health\")\n\nreturn {\n    init = function(state)\n        state.health = state.health or 3 -- Default player health\n\n        sucata.events.on(state, \"meteor_reached\", function(_)\n            health.remove(state)\n\n            if state.health &lt;= 0 then\n                -- Game over logic (to be implemented)\n            end\n        end)\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    PlayerHealth = require(\"behaviours.player_health\")\n}\n</code></pre> <p>Add it to the Game Manager in <code>entities/game_manager.lua</code>:</p> <pre><code>return {\n    state = {},\n    behaviours = {\n        Behaviours.PlayerHealth,\n        Behaviours.MeteorSpawner\n    }\n}\n</code></pre>"},{"location":"getting-started/first-project/game-manager/#player-points","title":"Player Points","text":"<p>Next, we will implement the player scoring system.</p> <p>Create the state module <code>states/player_points.lua</code>:</p> <pre><code>local function add_points(state, points)\n    if not state.points then -- Safe state modification\n        return\n    end\n\n    state.points = state.points + points\nend\n\nreturn {\n    add_points = add_points\n}\n</code></pre> <p>Now create the behaviour <code>behaviours/player_points.lua</code>:</p> <pre><code>local player_points = require(\"states.player_points\")\n\nreturn {\n    init = function(state)\n        state.points = 0 -- Initial score\n\n        sucata.events.on(state, \"meteor_destroyed\", function(_)\n            player_points.add_points(state, 5)\n        end)\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    PlayerPoints = require(\"behaviours.player_points\")\n}\n</code></pre> <p>Update the Game Manager in <code>entities/game_manager.lua</code>:</p> <pre><code>return {\n    state = {},\n    behaviours = {\n        Behaviours.PlayerHealth,\n        Behaviours.PlayerPoints,\n        Behaviours.MeteorSpawner\n    }\n}\n</code></pre>"},{"location":"getting-started/first-project/game-manager/#drawing-the-ui","title":"Drawing the UI","text":"<p>Finally, we will draw the player UI on the screen.</p> <p>Create the behaviour <code>behaviours/draw_player_ui.lua</code>:</p> <pre><code>return {\n    draw = function(state)\n        sucata.graphic.draw_text({\n            text = \"Health: \" .. state.health,\n            x = 960 - 16,\n            y = 10,\n            font_size = 16,\n            align = \"right\"\n        })\n\n        sucata.graphic.draw_text({\n            text = \"Points: \" .. state.points,\n            x = 960 - 16,\n            y = 40,\n            font_size = 16,\n            align = \"right\"\n        })\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    DrawPlayerUi = require(\"behaviours.draw_player_ui\")\n}\n</code></pre> <p>Add the UI behaviour to the Game Manager:</p> <pre><code>return {\n    state = {},\n    behaviours = {\n        Behaviours.PlayerHealth,\n        Behaviours.PlayerPoints,\n        Behaviours.MeteorSpawner,\n        Behaviours.DrawPlayerUi -- Rendering should happen last\n    }\n}\n</code></pre> <p>The UI should now appear like this:</p> <p></p>"},{"location":"getting-started/first-project/overview/","title":"First Project","text":"<p>In this section, we will create our first simple project using the Sucata Engine.</p>"},{"location":"getting-started/first-project/overview/#idea","title":"Idea","text":"<p>The goal of this project is to build a simple Asteroids-style game.</p> <p>The player will control a ship that can move and shoot bullets at incoming asteroids. To survive, the player must destroy the asteroids before they hit the ground.</p> <p>This project will introduce some basic concepts of the engine, such as:</p> <ul> <li>Creating entities</li> <li>Adding behaviors</li> <li>Handling movement</li> <li>Spawning objects</li> <li>Detecting collisions</li> <li>Creating UI</li> <li>Handling button clicks</li> </ul>"},{"location":"getting-started/first-project/playing-sounds/","title":"Playing Sounds","text":"<p>Now let's add sound effects and music to our game.</p> <p>All audio assets used in this tutorial are CC0 and were obtained from freesound.</p>"},{"location":"getting-started/first-project/playing-sounds/#health-loss-sound","title":"Health Loss Sound","text":"<p>This sound will play when a meteor reaches the end of the screen.</p> <p>Update the behaviour <code>behaviours/meteor.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        sucata.scene.add_tag(state, \"meteor\")\n\n        state.speed = state.speed or math.random(100, 200)\n        state.health = state.health or math.random(1, 5)\n        state.force_y = state.speed\n    end,\n\n    tick = function(state)\n        if state.y &gt; 540 then\n            sucata.events.emit(\"meteor_reached\", state)\n\n            sucata.audio.play({\n                sound = \"src://sounds/lose.ogg\"\n            })\n\n            sucata.scene.destroy(state)\n        end\n\n        state.atlas_x = state.health - 1\n    end\n}\n</code></pre> <p>Note <code>src://</code> represents the root directory of the project. Use this prefix whenever referencing files inside the project.</p>"},{"location":"getting-started/first-project/playing-sounds/#shooting-sound","title":"Shooting Sound","text":"<p>This sound will play when the player fires a bullet.</p> <p>Update <code>behaviours/bullet.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        state.speed = state.speed or 400\n        state.force_y = -state.speed\n\n        -- Play shooting sound when the bullet is spawned\n        sucata.audio.play({\n            sound = \"src://sounds/shoot.ogg\"\n        })\n    end,\n    ...\n}\n</code></pre>"},{"location":"getting-started/first-project/playing-sounds/#meteor-hit-sound","title":"Meteor Hit Sound","text":"<p>Now we will add a sound effect when a meteor is hit by a bullet.</p> <p>Update <code>behaviours/bullet.lua</code>:</p> <pre><code>return {\n    tick = function(state)\n        local meteors = sucata.scene.get_entities_by_tag(\"meteor\")\n\n        for _, id in ipairs(meteors) do\n            local meteor = sucata.scene.find_by_id(id)\n\n            if meteor and sucata.math.overlapping({\n                x = state.x - 8,\n                y = state.y - 8,\n                width = 16,\n                height = 16\n            }, {\n                x = meteor.x - 16,\n                y = meteor.y - 16,\n                width = 32,\n                height = 32\n            }) then\n                health.remove(meteor)\n\n                if meteor.health &lt;= 0 then\n                    sucata.events.emit(\"meteor_destroyed\", meteor)\n                    sucata.scene.destroy(meteor)\n                end\n\n                -- Play shooting sound when the meteor is hitted\n                sucata.audio.play({\n                    sound = \"src://sounds/explosion.ogg\"\n                })\n\n                sucata.scene.destroy(state)\n                break\n            end\n        end\n    end\n}\n</code></pre>"},{"location":"getting-started/first-project/playing-sounds/#playing-music","title":"Playing Music","text":"<p>Now we will add background music to the game.</p> <p>Create a new behaviour <code>behaviours/music.lua</code>:</p> <pre><code>return {\n    init = function(state)\n        if state.music then\n            state.music_id = sucata.audio.play({\n                sound = state.music,\n                loop = true,\n                volume = 0.5\n            })\n        end\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <p>```lua id=\"e5vwy2\" return {     ...     Music = require(\"behaviours.music\"), }</p> <pre><code>\nNow create an entity responsible for playing the background music.\n\nCreate `entities/music.lua`:\n\n```lua id=\"h53grq\"\nlocal function music()\n    return {\n        state = {\n            music = \"src://sounds/music.ogg\"\n        },\n\n        behaviours = {\n            Behaviours.Music\n        }\n    }\nend\n\nreturn music\n</code></pre> <p>Finally, spawn the music entity in <code>main.lua</code>:</p> <pre><code>local music = require(\"entities.music\")\n\nsucata.scene.spawn(music())\n</code></pre> <p>The background music will now play in a loop while the game is running.</p>"},{"location":"getting-started/first-project/shooting-meteors/","title":"Shooting Meteors","text":"<p>Now it's time to shoot some meteors. In this section we will create bullets and implement the logic required to destroy meteors and earn points.</p>"},{"location":"getting-started/first-project/shooting-meteors/#creating-the-bullet-behaviour","title":"Creating the Bullet Behaviour","text":"<p>First, create the bullet behaviour in <code>behaviours/bullet.lua</code>:</p> <pre><code>local health = require(\"states.health\")\n\nreturn {\n    init = function(state)\n        state.speed = state.speed or 400 -- Bullet speed (default: 400)\n        state.force_y = -state.speed -- Apply upward force\n    end,\n\n    tick = function(state)\n        -- Destroy bullet when it leaves the screen\n        if state.y &lt; -16 then\n            sucata.scene.destroy(state)\n        end\n\n        -- Get all meteors in the scene\n        local meteors = sucata.scene.get_entities_by_tag(\"meteor\")\n\n        for _, id in ipairs(meteors) do\n            local meteor = sucata.scene.find_by_id(id)\n\n            if meteor and sucata.math.overlapping({\n                x = state.x - 8,\n                y = state.y - 8,\n                width = 16,\n                height = 16\n            }, {\n                x = meteor.x - 16,\n                y = meteor.y - 16,\n                width = 32,\n                height = 32\n            }) then\n                -- Damage the meteor\n                health.remove(meteor)\n\n                if meteor.health &lt;= 0 then\n                    sucata.events.emit(\"meteor_destroyed\", meteor)\n                    sucata.scene.destroy(meteor)\n                end\n\n                -- Destroy the bullet\n                sucata.scene.destroy(state)\n                break\n            end\n        end\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    Bullet = require(\"behaviours.bullet\"),\n}\n</code></pre>"},{"location":"getting-started/first-project/shooting-meteors/#creating-the-bullet-entity","title":"Creating the Bullet Entity","text":"<p>Now create the bullet entity in <code>entities/bullet.lua</code>:</p> <pre><code>local function bullet(x, y)\n    return {\n        state = {\n            x = x,\n            y = y\n        },\n\n        behaviours = {\n            Behaviours.Bullet,      -- Bullet logic\n            Behaviours.ApplyForces, -- Apply movement forces\n            Behaviours.DrawSprite   -- Render the bullet\n        }\n    }\nend\n\nreturn bullet\n</code></pre>"},{"location":"getting-started/first-project/shooting-meteors/#creating-the-shooter-behaviour","title":"Creating the Shooter Behaviour","text":"<p>Now we will allow the player to shoot bullets.</p> <p>Create the file <code>behaviours/shooter.lua</code>:</p> <pre><code>local bullet = require(\"entities.bullet\")\n\nreturn {\n    tick = function(state)\n        if sucata.input.is_pressed(\"space\", \"enter\") then\n            sucata.scene.spawn(bullet(state.x, state.y - 16))\n        end\n    end\n}\n</code></pre> <p>Register the behaviour in <code>behaviours/init.lua</code>:</p> <pre><code>return {\n    ...\n    Shooter = require(\"behaviours.shooter\"),\n}\n</code></pre>"},{"location":"getting-started/first-project/shooting-meteors/#adding-the-shooter-to-the-player","title":"Adding the Shooter to the Player","text":"<p>Now add the shooter behaviour to the player entity in <code>entities/player.lua</code>:</p> <pre><code>local function player(x, y)\n    return {\n        state = {\n            x = x,\n            y = y\n        },\n\n        behaviours = {\n            Behaviours.Player,   -- Player movement\n            Behaviours.Shooter,  -- Shooting logic\n            Behaviours.DrawSprite -- Render player\n        }\n    }\nend\n\nreturn player\n</code></pre> <p>Note Behaviours are executed in order. The player logic runs first, followed by shooting logic, and finally rendering.</p> <p>Now when you run the game, it should look like this:</p> <p></p>"},{"location":"references/sucata.audio/","title":"sucata.audio","text":"<p>The audio module of the Sucata.</p>"},{"location":"references/sucata.audio/#sucataaudioplay","title":"sucata.audio.play","text":"<p>Plays a sound and returns its ID.</p> <p>parameters</p> <ul> <li>props <code>table</code> - Properties for the audio</li> <li>sound <code>string</code> - Path to the sound file</li> <li>volume? <code>number</code> - Volume of the sound (default: 1.0)</li> <li>pitch? <code>number</code> - Pitch of the sound (default: 1.0)</li> <li>group? <code>string</code> - Audio group (default: \"default\")</li> <li>loop? <code>boolean</code> - Whether the sound should loop (default: false)</li> </ul> <p>return</p> <ul> <li>sound_id <code>number</code> - The ID of the playing sound</li> </ul>"},{"location":"references/sucata.audio/#sucataaudiostop","title":"sucata.audio.stop","text":"<p>Stops a playing sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound to stop</li> </ul>"},{"location":"references/sucata.audio/#sucataaudiopause","title":"sucata.audio.pause","text":"<p>Pauses a playing sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound to pause</li> </ul>"},{"location":"references/sucata.audio/#sucataaudiounpause","title":"sucata.audio.unpause","text":"<p>Unpauses a paused sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound to unpause</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioget_volume","title":"sucata.audio.get_volume","text":"<p>Gets the volume of a specific sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound</li> </ul> <p>return</p> <ul> <li>volume <code>number</code> - The current volume of the sound</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioset_volume","title":"sucata.audio.set_volume","text":"<p>Sets the volume of a specific sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound</li> <li>volume <code>number</code> - The new volume level</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioget_pitch","title":"sucata.audio.get_pitch","text":"<p>Gets the pitch of a specific sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound</li> </ul> <p>return</p> <ul> <li>pitch <code>number</code> - The current pitch of the sound</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioset_pitch","title":"sucata.audio.set_pitch","text":"<p>Sets the pitch of a specific sound.</p> <p>parameters</p> <ul> <li>sound_id <code>number</code> - The ID of the sound</li> <li>pitch <code>number</code> - The new pitch level</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioget_group_volume","title":"sucata.audio.get_group_volume","text":"<p>Gets the volume of an audio group.</p> <p>parameters</p> <ul> <li>group_id <code>string</code> - The ID of the audio group</li> </ul> <p>return</p> <ul> <li>volume <code>number</code> - The current volume of the group</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioset_group_volume","title":"sucata.audio.set_group_volume","text":"<p>Sets the volume of an audio group.</p> <p>parameters</p> <ul> <li>group_id <code>string</code> - The ID of the audio group</li> <li>volume <code>number</code> - The new volume level</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioget_group_pitch","title":"sucata.audio.get_group_pitch","text":"<p>Gets the pitch of an audio group.</p> <p>parameters</p> <ul> <li>group_id <code>string</code> - The ID of the audio group</li> </ul> <p>return</p> <ul> <li>pitch <code>number</code> - The current pitch of the group</li> </ul>"},{"location":"references/sucata.audio/#sucataaudioset_group_pitch","title":"sucata.audio.set_group_pitch","text":"<p>Sets the pitch of an audio group.</p> <p>parameters</p> <ul> <li>group_id <code>string</code> - The ID of the audio group</li> <li>pitch <code>number</code> - The new pitch level</li> </ul>"},{"location":"references/sucata.camera/","title":"sucata.camera","text":"<p>Camera module of the Sucata.</p>"},{"location":"references/sucata.camera/#sucatacameraget_camera_position","title":"sucata.camera.get_camera_position","text":"<p>Gets the current camera position.</p> <p>return</p> <ul> <li>x <code>number</code> - X coordinate of the camera position  </li> <li>y <code>number</code> - Y coordinate of the camera position  </li> </ul>"},{"location":"references/sucata.camera/#sucatacameraset_camera_position","title":"sucata.camera.set_camera_position","text":"<p>Sets the camera position.</p> <p>parameters</p> <ul> <li>x <code>number</code> - X coordinate of the camera position  </li> <li>y <code>number</code> - Y coordinate of the camera position  </li> </ul>"},{"location":"references/sucata.camera/#sucatacameraget_camera_rotation","title":"sucata.camera.get_camera_rotation","text":"<p>Gets the current camera rotation.</p> <p>return</p> <ul> <li>rotation <code>number</code> - Camera rotation in radians  </li> </ul>"},{"location":"references/sucata.camera/#sucatacameraset_camera_rotation","title":"sucata.camera.set_camera_rotation","text":"<p>Sets the camera rotation.</p> <p>parameters</p> <ul> <li>rotation <code>number</code> - Camera rotation in radians  </li> </ul>"},{"location":"references/sucata.camera/#sucatacameraget_camera_zoom","title":"sucata.camera.get_camera_zoom","text":"<p>Gets the current camera zoom.</p> <p>return</p> <ul> <li>zoom <code>number</code> - Camera zoom level  </li> </ul>"},{"location":"references/sucata.camera/#sucatacameraset_camera_zoom","title":"sucata.camera.set_camera_zoom","text":"<p>Sets the camera zoom.</p> <p>parameters</p> <ul> <li>zoom <code>number</code> - Camera zoom level  </li> </ul>"},{"location":"references/sucata.dynlib/","title":"sucata.dynlib","text":"<p>Load native shared libraries (<code>.dll</code>/<code>.so</code>/<code>.dylib</code>) at runtime and call functions exported by them.</p> <p>This is a low-level API meant for native plugins, not regular game code. Exported functions must be written against the Lua 5.4 C API (<code>lua.h</code>), with the exact signature <code>int (*)(lua_State *L)</code> \u2014 the same signature every <code>sucata.*</code> function uses internally. When called, the plugin function receives the real, live Lua state: read arguments and push return values using the Lua C API (<code>lua_tonumber</code>, <code>lua_tostring</code>, <code>lua_pushnumber</code>, <code>luaL_error</code>, ...), exactly like a standard Lua C module (this mirrors what <code>package.loadlib</code> does in stock Lua).</p> <p>Important: the plugin must not statically link its own copy of the Lua library. It should only include the Lua headers and leave the <code>lua_*</code>/<code>luaL_*</code> symbols unresolved at link time \u2014 the Sucata Player process exports them at runtime, so <code>dlopen</code>/<code>LoadLibrary</code> resolves the plugin's calls against the same Lua instance the engine is already running. Linking a second, separate copy of Lua into the plugin will desync the interpreter state and can crash the engine, especially across <code>luaL_error</code>/<code>lua_error</code>.</p> <p>Minimal example plugin (<code>plugin.c</code>, compiled with <code>gcc -shared -fPIC -o plugin.so plugin.c</code>, no <code>-llua</code>):</p> <pre><code>#include \"lua.h\"\n#include \"lauxlib.h\"\n\nint add(lua_State *L) {\n    lua_Number a = lua_tonumber(L, 1);\n    lua_Number b = lua_tonumber(L, 2);\n    lua_pushnumber(L, a + b);\n    return 1;\n}\n</code></pre> <pre><code>local handle, err = sucata.dynlib.load(\"plugin.so\")\nif not handle then\n    error(err)\nend\n\nlocal sum = sucata.dynlib.call(handle, \"add\", 3, 4)\nprint(sum) -- 7\n\nsucata.dynlib.unload(handle)\n</code></pre>"},{"location":"references/sucata.dynlib/#sucatadynlibload","title":"sucata.dynlib.load","text":"<p>Loads a native shared library from disk and returns a handle to it. Accepts <code>src://</code>, <code>data://</code>, <code>build://</code> virtual paths (resolved the same way as <code>sucata.filesystem</code>) as well as plain relative or absolute paths. If the path has no extension, the platform's native extension (<code>.dll</code>/<code>.so</code>/<code>.dylib</code>) is tried as a fallback.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path to the shared library</li> </ul> <p>return</p> <ul> <li>handle <code>number|nil</code> - A handle to the loaded library, or <code>nil</code> on failure</li> <li>error <code>string?</code> - An error message, present only when <code>handle</code> is <code>nil</code></li> </ul>"},{"location":"references/sucata.dynlib/#sucatadynlibcall","title":"sucata.dynlib.call","text":"<p>Looks up an exported symbol in a loaded library and calls it directly as a Lua C function, forwarding every extra argument to it on the Lua stack. Returns whatever the native function pushes.</p> <p>Raises a Lua error if the handle is invalid or the symbol isn't found. If the native function itself raises a Lua error (<code>luaL_error</code>/<code>lua_error</code>), it propagates like any other Lua error and can be caught with <code>pcall</code>.</p> <p>parameters</p> <ul> <li>handle <code>number</code> - A handle returned by <code>sucata.dynlib.load</code></li> <li>function_name <code>string</code> - The name of the exported function to call</li> <li>... <code>any</code> - Arguments forwarded to the native function</li> </ul> <p>return</p> <ul> <li>... <code>any</code> - Whatever the native function returns</li> </ul>"},{"location":"references/sucata.dynlib/#sucatadynlibunload","title":"sucata.dynlib.unload","text":"<p>Unloads a previously loaded library and invalidates its handle.</p> <p>parameters</p> <ul> <li>handle <code>number</code> - A handle returned by <code>sucata.dynlib.load</code></li> </ul> <p>return</p> <ul> <li>success <code>boolean</code> - Whether the library was unloaded</li> </ul>"},{"location":"references/sucata.events/","title":"sucata.events","text":"<p>The events module of the Sucata.</p>"},{"location":"references/sucata.events/#sucataeventsemit","title":"sucata.events.emit","text":"<p>Emits an event with the given name and data.</p> <p>parameters</p> <ul> <li>name <code>string</code> - The name of the event  </li> <li>data <code>table</code> - Additional data to pass with the event  </li> </ul>"},{"location":"references/sucata.events/#sucataeventson","title":"sucata.events.on","text":"<p>Registers a handler for an event.</p> <p>parameters</p> <ul> <li>owner <code>number | Entity</code> - The entity owner of the handler (can be an entity table or entity ID)  </li> <li>name <code>string</code> - The name of the event  </li> <li>callback <code>function</code> - The function to call when the event is emitted  </li> </ul>"},{"location":"references/sucata.filesystem/","title":"sucata.filesystem","text":"<p>The filesystem module of the Sucata.</p>"},{"location":"references/sucata.filesystem/#sucatafilesystemexists","title":"sucata.filesystem.exists","text":"<p>Checks if a file or directory exists.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path of the file or directory to check  </li> </ul> <p>return</p> <ul> <li>exists <code>boolean</code> - <code>true</code> if the file/directory exists, <code>false</code> otherwise  </li> </ul>"},{"location":"references/sucata.filesystem/#sucatafilesystemremove","title":"sucata.filesystem.remove","text":"<p>Removes a file or directory.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path of the file or directory to remove  </li> </ul>"},{"location":"references/sucata.filesystem/#sucatafilesystemmkdir","title":"sucata.filesystem.mkdir","text":"<p>Creates a new directory.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path of the directory to create  </li> </ul>"},{"location":"references/sucata.filesystem/#sucatafilesystemread_file","title":"sucata.filesystem.read_file","text":"<p>Reads the contents of a file as a string.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path of the file to read  </li> </ul> <p>return</p> <ul> <li>content <code>string | nil</code> - The file contents as a string, or <code>nil</code> on error  </li> </ul>"},{"location":"references/sucata.filesystem/#sucatafilesystemread_dir","title":"sucata.filesystem.read_dir","text":"<p>Lists the contents of a directory.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path of the directory to read  </li> </ul> <p>return</p> <ul> <li>files <code>table | nil</code> - Table with file/directory names, or <code>nil</code> on error  </li> </ul>"},{"location":"references/sucata.filesystem/#sucatafilesystemwrite","title":"sucata.filesystem.write","text":"<p>Writes content to a file.</p> <p>parameters</p> <ul> <li>path <code>string</code> - Path of the file to write  </li> <li>content <code>string</code> - Content to write to the file  </li> </ul> <p>return</p> <ul> <li>success <code>boolean</code> - <code>true</code> if the write was successful, <code>false</code> otherwise  </li> </ul>"},{"location":"references/sucata.filesystem/#sucatafilesystemrename","title":"sucata.filesystem.rename","text":"<p>Renames a file or directory.</p> <p>parameters</p> <ul> <li>old_path <code>string</code> - Current path of the file or directory  </li> <li>new_path <code>string</code> - New path of the file or directory  </li> </ul>"},{"location":"references/sucata.gamepad/","title":"sucata.gamepad","text":"<p>The gamepad module of the Sucata.</p>"},{"location":"references/sucata.gamepad/#axis","title":"Axis","text":"<p>Gamepad axis and trigger names.</p> <p>values</p> <ul> <li><code>\"left_x\"</code></li> <li><code>\"left_y\"</code></li> <li><code>\"right_x\"</code></li> <li><code>\"right_y\"</code></li> <li><code>\"trigger_left\"</code></li> <li><code>\"trigger_right\"</code></li> </ul>"},{"location":"references/sucata.gamepad/#button","title":"Button","text":"<p>Gamepad button names.</p> <p>values</p> <ul> <li><code>\"a\"</code></li> <li><code>\"b\"</code></li> <li><code>\"x\"</code></li> <li><code>\"y\"</code></li> <li><code>\"back\"</code></li> <li><code>\"guide\"</code></li> <li><code>\"start\"</code></li> <li><code>\"left_stick\"</code></li> <li><code>\"right_stick\"</code></li> <li><code>\"left_shoulder\"</code></li> <li><code>\"right_shoulder\"</code></li> <li><code>\"dpad_up\"</code></li> <li><code>\"dpad_down\"</code></li> <li><code>\"dpad_left\"</code></li> <li><code>\"dpad_right\"</code></li> </ul>"},{"location":"references/sucata.gamepad/#sucatagamepadget_count","title":"sucata.gamepad.get_count","text":"<p>Gets the number of currently connected gamepads.</p> <p>return</p> <ul> <li>count <code>number</code> - The number of gamepads connected  </li> </ul>"},{"location":"references/sucata.gamepad/#sucatagamepadget_info","title":"sucata.gamepad.get_info","text":"<p>Gets the human-readable name of a gamepad device.</p> <p>parameters</p> <ul> <li>device <code>number</code> - The gamepad device slot  </li> </ul> <p>return</p> <ul> <li>name <code>string</code> - The name reported by the gamepad driver for the given device  </li> </ul>"},{"location":"references/sucata.gamepad/#sucatagamepadget_axis","title":"sucata.gamepad.get_axis","text":"<p>Gets the axis value of a gamepad.</p> <p>parameters</p> <ul> <li>axis <code>Axis</code> - The gamepad axis  </li> <li>device? <code>number</code> - The number of the device (optional)  </li> </ul> <p>return</p> <ul> <li>value <code>number</code> - The value of the axis  </li> <li>device_used <code>number</code> - The device used  </li> </ul>"},{"location":"references/sucata.gamepad/#sucatagamepadis_held","title":"sucata.gamepad.is_held","text":"<p>Checks if a button is currently held on the gamepad.</p> <p>parameters</p> <ul> <li>button <code>Button</code> - The gamepad button  </li> <li>device? <code>number</code> - The number of the device (optional)  </li> </ul> <p>return</p> <ul> <li>value <code>boolean</code> - <code>true</code> if the button is currently held  </li> <li>device_used <code>number</code> - The device used  </li> </ul>"},{"location":"references/sucata.gamepad/#sucatagamepadis_pressed","title":"sucata.gamepad.is_pressed","text":"<p>Checks if a button was pressed on the gamepad (only on the frame it was pressed).</p> <p>parameters</p> <ul> <li>button <code>Button</code> - The gamepad button  </li> <li>device? <code>number</code> - The number of the device (optional)  </li> </ul> <p>return</p> <ul> <li>value <code>boolean</code> - <code>true</code> if the button is pressed  </li> <li>device_used <code>number</code> - The device used  </li> </ul>"},{"location":"references/sucata.gamepad/#sucatagamepadis_released","title":"sucata.gamepad.is_released","text":"<p>Checks if a button was released on the gamepad.</p> <p>parameters</p> <ul> <li>button <code>Button</code> - The gamepad button  </li> <li>device? <code>number</code> - The number of the device (optional)  </li> </ul> <p>return</p> <ul> <li>value <code>boolean</code> - <code>true</code> if the button is released  </li> <li>device_used <code>number</code> - The device used  </li> </ul>"},{"location":"references/sucata.graphic/","title":"sucata.graphic","text":"<p>The graphic module of the Sucata.</p>"},{"location":"references/sucata.graphic/#rectprops","title":"RectProps","text":"<p>Properties used to draw a rectangle.</p> <p>fields - x? <code>number</code> - The x position (default: 0) - y? <code>number</code> - The y position (default: 0) - width? <code>number</code> - The width (default: 50) - height? <code>number</code> - The height (default: 50) - color? <code>string</code> - The color in hex format (default: \"#ffffff\") - z_index? <code>number</code> - The z-index for layering (default: 0) - texture? <code>string</code> - Path to texture file (default: \"\") - scale? <code>number</code> - Uniform scale factor (default: 1.0) - scale_x? <code>number</code> - Scale factor on X axis (default: 1.0) - scale_y? <code>number</code> - Scale factor on Y axis (default: 1.0) - fixed? <code>boolean</code> - Whether the rectangle is fixed to the screen (default: false) - tiled? <code>boolean</code> - Whether the texture should be tiled (default: false) - tile_size? <code>number</code> - Uniform tile size for tiling (default: 0.0) - tile_width? <code>number</code> - Tile width on X axis for tiling (default: 0.0) - tile_height? <code>number</code> - Tile height on Y axis for tiling (default: 0.0) - origin? <code>number</code> - Uniform origin point (default: 0.0) - origin_x? <code>number</code> - Origin point on X axis (default: 0.0) - origin_y? <code>number</code> - Origin point on Y axis (default: 0.0) - rotation? <code>number</code> - Rotation in radians (default: 0.0) - opacity? <code>number</code> - Opacity value from 0.0 to 1.0 (default: the alpha channel of <code>color</code>) - atlas_size? <code>number</code> - Size of the texture atlas (default: 0.0) - atlas_width? <code>number</code> - Width of each frame in the texture atlas (default: 0.0) - atlas_height? <code>number</code> - Height of each frame in the texture atlas (default: 0.0) - atlas_spacing? <code>number</code> - Spacing between frames in the texture atlas (default: 0.0) - atlas_margin? <code>number</code> - Margin around the frames in the texture atlas (default: 0.0) - atlas_x? <code>number</code> - X index of the frame in the texture atlas (default: 0.0) - atlas_y? <code>number</code> - Y index of the frame in the texture atlas (default: 0.0) - shader? <code>number</code> - ID of the shader to use, returned by <code>load_shader</code> (default: 0) - shader_args? <code>table</code> - Arguments for shader rendering (default: {})  </p>"},{"location":"references/sucata.graphic/#textprops","title":"TextProps","text":"<p>Properties used to draw text.</p> <p>fields - x? <code>number</code> - The x position (default: 0) - y? <code>number</code> - The y position (default: 0) - text? <code>string</code> - The text to display (default: \"\") - size? <code>number</code> - The font size (default: 16) - font? <code>string</code> - Path to font file (default: \"\") - color? <code>string</code> - The color in hex format (default: \"#ffffff\") - z_index? <code>number</code> - The z-index for layering (default: 0) - scale? <code>number</code> - Uniform scale factor (default: 1.0) - scale_x? <code>number</code> - Scale factor on X axis (default: 1.0) - scale_y? <code>number</code> - Scale factor on Y axis (default: 1.0) - fixed? <code>boolean</code> - Whether the text is fixed to the screen (default: false) - origin? <code>number</code> - Uniform origin point (default: 0.0) - origin_x? <code>number</code> - Origin point on X axis (default: 0.0) - origin_y? <code>number</code> - Origin point on Y axis (default: 0.0) - rotation? <code>number</code> - Rotation in radians (default: 0.0) - opacity? <code>number</code> - Opacity value from 0.0 to 1.0 (default: the alpha channel of <code>color</code>) - align? <code>string</code> - Text alignment: <code>\"left\"</code>, <code>\"center\"</code>, or <code>\"right\"</code> (default: <code>\"left\"</code>) - max_width? <code>number</code> - Maximum width of the text (default: 0.0) - shader? <code>number</code> - ID of the shader to use, returned by <code>load_shader</code> (default: 0) - shader_args? <code>table</code> - Arguments for shader rendering (default: {})  </p>"},{"location":"references/sucata.graphic/#sucatagraphicdraw_rect","title":"sucata.graphic.draw_rect","text":"<p>Draws a rectangle.</p> <p>parameters</p> <ul> <li>props <code>RectProps</code> - Properties for the rectangle  </li> </ul>"},{"location":"references/sucata.graphic/#sucatagraphicdraw_text","title":"sucata.graphic.draw_text","text":"<p>Draws text.</p> <p>parameters</p> <ul> <li>props <code>TextProps</code> - Properties for the text  </li> </ul>"},{"location":"references/sucata.graphic/#sucatagraphicset_background_color","title":"sucata.graphic.set_background_color","text":"<p>Sets the background color of the window.</p> <p>parameters</p> <ul> <li>color <code>string</code> - Hex color string (e.g., <code>\"#RRGGBB\"</code> or <code>\"#RRGGBBAA\"</code>)  </li> </ul>"},{"location":"references/sucata.graphic/#sucatagraphicload_shader","title":"sucata.graphic.load_shader","text":"<p>Loads a Sucata shader.</p> <p>parameters</p> <ul> <li>path <code>string</code> - File path to the Sucata shader  </li> </ul> <p>return</p> <ul> <li>shader_id <code>number | nil</code> - The ID of the loaded shader, or <code>nil</code> if loading failed  </li> </ul>"},{"location":"references/sucata.graphic/#sucatagraphicadd_post_processing","title":"sucata.graphic.add_post_processing","text":"<p>Adds a post-processing effect using a loaded shader.</p> <p>parameters</p> <ul> <li>shader_id <code>number</code> - The shader ID returned by <code>load_shader</code> </li> </ul>"},{"location":"references/sucata.graphic/#sucatagraphicset_post_processing_args","title":"sucata.graphic.set_post_processing_args","text":"<p>Sets a parameter on an active post-processing shader.</p> <p>parameters</p> <ul> <li>shader_id <code>number</code> - The shader ID of the post-processing effect  </li> <li>field <code>string</code> - The name of the shader argument to set  </li> <li>value <code>number | string | table</code> - The value to set (<code>number</code> for float, <code>string</code> for string uniform, <code>table</code> of 2/3/4 numbers for vec2/vec3/vec4)  </li> </ul>"},{"location":"references/sucata.graphic/#sucatagraphicremove_post_processing","title":"sucata.graphic.remove_post_processing","text":"<p>Removes an active post-processing effect.</p> <p>parameters</p> <ul> <li>shader_id <code>number</code> - The shader ID of the post-processing effect to remove  </li> </ul>"},{"location":"references/sucata.input/","title":"sucata.input","text":"<p>The input module of the Sucata.</p>"},{"location":"references/sucata.input/#key","title":"Key","text":"<p>Input key and button names.</p> <p>values</p> <ul> <li><code>\"mouse_left\"</code></li> <li><code>\"mouse_right\"</code></li> <li><code>\"mouse_middle\"</code></li> <li><code>\"a\"</code> to <code>\"z\"</code></li> <li><code>\"space\"</code></li> <li><code>\" \"</code> (space character)</li> <li><code>\"escape\"</code></li> <li><code>\"esc\"</code></li> <li><code>\"enter\"</code></li> <li><code>\"return\"</code></li> <li><code>\"shift\"</code></li> <li><code>\"ctrl\"</code></li> <li><code>\"control\"</code></li> <li><code>\"alt\"</code></li> <li><code>\"apostrophe\"</code></li> <li><code>\"tab\"</code></li> <li><code>\"up\"</code></li> <li><code>\"down\"</code></li> <li><code>\"left\"</code></li> <li><code>\"right\"</code></li> </ul>"},{"location":"references/sucata.input/#sucatainputget_mouse_position","title":"sucata.input.get_mouse_position","text":"<p>Gets the current mouse position.</p> <p>return</p> <ul> <li>x <code>number</code> - The x coordinate of the mouse  </li> <li>y <code>number</code> - The y coordinate of the mouse  </li> </ul>"},{"location":"references/sucata.input/#sucatainputget_relative_mouse_position","title":"sucata.input.get_relative_mouse_position","text":"<p>Gets the current mouse position based on the camera position, rotation and zoom.</p> <p>return</p> <ul> <li>x <code>number</code> - The x coordinate of the mouse  </li> <li>y <code>number</code> - The y coordinate of the mouse  </li> </ul>"},{"location":"references/sucata.input/#sucatainputget_mouse_scroll","title":"sucata.input.get_mouse_scroll","text":"<p>Gets the current mouse scroll values.</p> <p>return</p> <ul> <li>x <code>number</code> - The horizontal scroll value  </li> <li>y <code>number</code> - The vertical scroll value  </li> </ul>"},{"location":"references/sucata.input/#sucatainputget_key","title":"sucata.input.get_key","text":"<p>Gets the last pressed key character code.</p> <p>return</p> <ul> <li>key_code <code>number</code> - The character code of the last pressed key  </li> </ul>"},{"location":"references/sucata.input/#sucatainputis_pressed","title":"sucata.input.is_pressed","text":"<p>Checks if any of the given keys or buttons were pressed this frame.</p> <p>parameters</p> <ul> <li>... <code>Key</code> - Key or button names to check  </li> </ul> <p>return</p> <ul> <li>pressed <code>boolean</code> - Whether any of the keys/buttons were pressed  </li> </ul>"},{"location":"references/sucata.input/#sucatainputis_held","title":"sucata.input.is_held","text":"<p>Checks if any of the given keys or buttons are currently held down.</p> <p>parameters</p> <ul> <li>... <code>Key</code> - Key or button names to check  </li> </ul> <p>return</p> <ul> <li>held <code>boolean</code> - Whether any of the keys/buttons are held  </li> </ul>"},{"location":"references/sucata.input/#sucatainputis_released","title":"sucata.input.is_released","text":"<p>Checks if any of the given keys or buttons were released this frame.</p> <p>parameters</p> <ul> <li>... <code>Key</code> - Key or button names to check  </li> </ul> <p>return</p> <ul> <li>released <code>boolean</code> - Whether any of the keys/buttons were released  </li> </ul>"},{"location":"references/sucata.input/#sucatainputis_hover","title":"sucata.input.is_hover","text":"<p>Registers an area as hoverable for the current frame and returns whether the mouse is currently over it. When multiple registered areas overlap, only the one with the highest <code>z_index</code> reports <code>true</code>, so call this every frame for each interactive region you want the mouse to reach (e.g. inside a <code>draw</code> behaviour).</p> <p>parameters</p> <ul> <li>area <code>table</code></li> <li>id <code>string</code> - Unique id, can be the entity id  </li> <li>x <code>number</code> - X position of the area  </li> <li>y <code>number</code> - Y position of the area  </li> <li>width <code>number</code> - Width of the area  </li> <li>height <code>number</code> - Height of the area  </li> <li>z_index? <code>number</code> - Z-index used to break ties between overlapping areas (optional)  </li> <li>fixed? <code>boolean</code> - Whether the area is fixed to the screen (optional)  </li> </ul> <p>return</p> <ul> <li>hovering <code>boolean</code> - Whether the mouse is hovering over this area (and no higher-z area covers it)  </li> </ul>"},{"location":"references/sucata.math/","title":"sucata.math","text":"<p>The math module of the Sucata.</p>"},{"location":"references/sucata.math/#rect","title":"Rect","text":"<p>Represents a rectangle in 2D space.</p> <p>fields - x? <code>number</code> - The x position (default: 0) - y? <code>number</code> - The y position (default: 0) - width? <code>number</code> - The width (default: 1) - height? <code>number</code> - The height (default: 1)  </p>"},{"location":"references/sucata.math/#screenrelativerect","title":"ScreenRelativeRect","text":"<p>Represents a rectangle using screen-relative percentages (0\u20131).</p> <p>fields - top? <code>number</code> - Top position as percentage (0\u20131) (default: 0) - left? <code>number</code> - Left position as percentage (0\u20131) (default: 0) - right? <code>number</code> - Right position as percentage (0\u20131) (default: 0) - bottom? <code>number</code> - Bottom position as percentage (0\u20131) (default: 0)  </p>"},{"location":"references/sucata.math/#sucatamathclamp","title":"sucata.math.clamp","text":"<p>Clamps a value between a minimum and maximum.</p> <p>parameters</p> <ul> <li>value <code>number</code> - The value to clamp  </li> <li>min <code>number</code> - The minimum value  </li> <li>max <code>number</code> - The maximum value  </li> </ul> <p>return</p> <ul> <li>clamped <code>number</code> - The clamped value  </li> </ul>"},{"location":"references/sucata.math/#sucatamathdistance","title":"sucata.math.distance","text":"<p>Calculates the distance between two points.</p> <p>parameters</p> <ul> <li>point1 <code>table</code> - First point with <code>x</code> and <code>y</code> fields  </li> <li>point2 <code>table</code> - Second point with <code>x</code> and <code>y</code> fields  </li> </ul> <p>return</p> <ul> <li>distance <code>number</code> - The distance between the points  </li> </ul>"},{"location":"references/sucata.math/#sucatamathlerp","title":"sucata.math.lerp","text":"<p>Performs linear interpolation between two values.</p> <p>parameters</p> <ul> <li>a <code>number</code> - The start value  </li> <li>b <code>number</code> - The end value  </li> <li>t <code>number</code> - The interpolation factor (0\u20131)  </li> </ul> <p>return</p> <ul> <li>result <code>number</code> - The interpolated value  </li> </ul>"},{"location":"references/sucata.math/#sucatamathoverlapping","title":"sucata.math.overlapping","text":"<p>Checks if two rectangles are overlapping and returns the intersection.</p> <p>parameters</p> <ul> <li>rect1 <code>Rect</code> - First rectangle with <code>x</code>, <code>y</code>, <code>width</code>, <code>height</code> </li> <li>rect2 <code>Rect</code> - Second rectangle with <code>x</code>, <code>y</code>, <code>width</code>, <code>height</code> </li> </ul> <p>return</p> <ul> <li>overlapping <code>boolean</code> - Whether the rectangles overlap  </li> <li>intersection <code>Rect | nil</code> - The intersection rectangle if overlapping, <code>nil</code> otherwise  </li> </ul>"},{"location":"references/sucata.math/#sucatamathscreen_relative","title":"sucata.math.screen_relative","text":"<p>Converts screen-relative coordinates to pixel coordinates.</p> <p>parameters</p> <ul> <li>rect <code>ScreenRelativeRect</code> - Rectangle with <code>top</code>, <code>left</code>, <code>right</code>, <code>bottom</code> as percentages (0\u20131)  </li> </ul> <p>return</p> <ul> <li>x <code>number</code> - The x position in pixels  </li> <li>y <code>number</code> - The y position in pixels  </li> <li>width <code>number</code> - The width in pixels  </li> <li>height <code>number</code> - The height in pixels  </li> </ul>"},{"location":"references/sucata.math/#sucatamathsmooth_index","title":"sucata.math.smooth_index","text":"<p>Converts time into integer steps based on an interval, wrapping the result so the index loops between <code>0</code> and <code>max_index</code>. Useful for stepping through animation frames at a fixed rate.</p> <p>parameters</p> <ul> <li>current_time <code>number</code> - The current time (accumulated delta time)  </li> <li>interval <code>number</code> - The interval time in seconds  </li> <li>max_index <code>number</code> - The maximum index \u2014 the result wraps around when it exceeds this value  </li> </ul> <p>return</p> <ul> <li>index <code>number</code> - The calculated integer index  </li> </ul>"},{"location":"references/sucata.math/#sucatamathnormalize","title":"sucata.math.normalize","text":"<p>Normalizes a vector (accepts a variable number of components).</p> <p>parameters</p> <ul> <li>... <code>number</code> - Vector components (<code>x</code>, <code>y</code>, <code>z</code>, etc.)  </li> </ul> <p>return</p> <ul> <li>... <code>number</code> - Normalized vector components (same count as input)  </li> </ul>"},{"location":"references/sucata.math/#sucatamathmove_towards","title":"sucata.math.move_towards","text":"<p>Moves a value towards a target by a maximum step.</p> <p>parameters</p> <ul> <li>current <code>number</code> - The current value  </li> <li>target <code>number</code> - The target value  </li> <li>step <code>number</code> - The maximum step to move per call  </li> </ul> <p>return</p> <ul> <li>result <code>number</code> - The new value moved towards the target  </li> </ul>"},{"location":"references/sucata.meta/","title":"sucata.meta","text":"<p>The meta module of the Sucata.</p>"},{"location":"references/sucata.meta/#sucatametaos","title":"sucata.meta.OS","text":"<p>It is the current OS</p>"},{"location":"references/sucata.meta/#sucatametaversion","title":"sucata.meta.VERSION","text":"<p>It is the current sucata version</p>"},{"location":"references/sucata.scene/","title":"sucata.scene","text":"<p>The scene module of the Sucata.</p>"},{"location":"references/sucata.scene/#behaviour","title":"Behaviour","text":"<p>Represents an script that changes the entity state</p> <p>fields</p> <ul> <li> <p>tick? <code>function(state)</code>   The tick function called every frame.   Receives <code>state</code> as parameter.</p> </li> <li> <p>draw? <code>function(state)</code>   The draw function called every frame.   Receives <code>state</code> as parameter.</p> </li> <li> <p>free? <code>function(state)</code>   The function called when the entity is destroyed.   Receives <code>state</code> as parameter.</p> </li> <li> <p>init? <code>function(state)</code>   The function called when the entity is spawned.   Receives <code>state</code> as parameter.</p> </li> </ul>"},{"location":"references/sucata.scene/#entity","title":"Entity","text":"<p>Represents a game object in the scene.</p> <p>An <code>Entity</code> can contain state and behaviours</p> <p>fields</p> <ul> <li> <p>state <code>table</code>   The unique state of the entity</p> </li> <li> <p>behaviours? <code>Behaviour[]</code>   The list of behaviours that entity will execute</p> </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneload_scene","title":"sucata.scene.load_scene","text":"<p>Loads a scene with the given entities.</p> <p>parameters</p> <ul> <li>entities <code>Entity[]</code> - Array of entity tables to load into the scene  </li> </ul>"},{"location":"references/sucata.scene/#sucatascenespawn","title":"sucata.scene.spawn","text":"<p>Spawns an entity in the scene.</p> <p>parameters</p> <ul> <li>entity <code>Entity</code> - The entity table to spawn  </li> </ul> <p>return</p> <ul> <li>entity_id <code>number</code> - The ID of the spawned entity  </li> </ul>"},{"location":"references/sucata.scene/#sucatascenespawns","title":"sucata.scene.spawns","text":"<p>Spawns multiple entities in the scene.</p> <p>parameters</p> <ul> <li>entities <code>Entity[]</code> - Array of entity tables to spawn  </li> </ul> <p>return</p> <ul> <li>entity_ids <code>number[]</code> - Array of IDs of the spawned entities  </li> </ul>"},{"location":"references/sucata.scene/#sucatascenefind_by_id","title":"sucata.scene.find_by_id","text":"<p>Finds an entity by its ID.</p> <p>parameters</p> <ul> <li>entity_id <code>number</code> - The ID of the entity to find  </li> </ul> <p>return</p> <ul> <li>entity <code>Entity | nil</code> - The entity table or <code>nil</code> if not found  </li> </ul>"},{"location":"references/sucata.scene/#sucatascenedestroy","title":"sucata.scene.destroy","text":"<p>Destroys an entity from the scene.</p> <p>parameters</p> <ul> <li>entity_or_id <code>Entity | number</code> - The entity table or entity ID to destroy  </li> </ul> <p>return</p> <ul> <li>success <code>boolean</code> - Whether the entity was successfully destroyed  </li> </ul>"},{"location":"references/sucata.scene/#sucatascenedestroys","title":"sucata.scene.destroys","text":"<p>Destroys multiple entities from the scene.</p> <p>parameters</p> <ul> <li>entities <code>Entity[]</code> - Array of entity tables to destroy  </li> </ul> <p>return</p> <ul> <li>undestroyed_ids <code>number[]</code> - Array of IDs of entities that could not be destroyed  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneadd_tag","title":"sucata.scene.add_tag","text":"<p>Adds a tag to an entity.</p> <p>parameters</p> <ul> <li>entity_or_id <code>Entity | number</code> - The entity table or entity ID  </li> <li>tag <code>string</code> - The tag to add  </li> </ul> <p>return</p> <ul> <li>success <code>boolean</code> - Whether the tag was successfully added  </li> </ul>"},{"location":"references/sucata.scene/#sucatascenehas_tag","title":"sucata.scene.has_tag","text":"<p>Checks if an entity has a tag.</p> <p>parameters</p> <ul> <li>entity_or_id <code>Entity | number</code> - The entity table or entity ID  </li> <li>tag <code>string</code> - The tag to check  </li> </ul> <p>return</p> <ul> <li>success <code>boolean</code> - Whether the entity has the tag  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneremove_tag","title":"sucata.scene.remove_tag","text":"<p>Removes a tag from an entity.</p> <p>parameters</p> <ul> <li>entity_or_id <code>Entity | number</code> - The entity table or entity ID  </li> <li>tag <code>string</code> - The tag to remove  </li> </ul> <p>return</p> <ul> <li>success <code>boolean</code> - Whether the tag was successfully removed  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneget_entities","title":"sucata.scene.get_entities","text":"<p>Gets all entity IDs in the scene.</p> <p>return</p> <ul> <li>entity_ids <code>number[]</code> - Array of all entity IDs in the scene  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneget_entities_by_tag","title":"sucata.scene.get_entities_by_tag","text":"<p>Gets all entity IDs with a specific tag.</p> <p>parameters</p> <ul> <li>tag <code>string</code> - The tag to filter by  </li> </ul> <p>return</p> <ul> <li>entity_ids <code>number[]</code> - Array of entity IDs with the given tag  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneclear_entities","title":"sucata.scene.clear_entities","text":"<p>Clears all entities from the scene.</p>"},{"location":"references/sucata.scene/#sucatasceneinit","title":"sucata.scene.init","text":"<p>Registers a callback to run when the scene starts. If the scene has already started, the callback is called immediately.</p> <p>parameters</p> <ul> <li>callback <code>function</code> - The function to call when the scene starts  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneload_global","title":"sucata.scene.load_global","text":"<p>Stores an entity globally so it persists across scene loads.</p> <p>parameters</p> <ul> <li>key <code>string</code> - The key to store the entity under  </li> <li>entity <code>Entity</code> - The entity to store  </li> </ul> <p>return</p> <ul> <li>entity_id <code>number</code> - The ID of the spawned global entity  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneget_global","title":"sucata.scene.get_global","text":"<p>Retrieves a globally stored entity by key.</p> <p>parameters</p> <ul> <li>key <code>string</code> - The key the entity was stored under  </li> </ul> <p>return</p> <ul> <li>entity <code>Entity | nil</code> - The global entity state, or <code>nil</code> if not found  </li> </ul>"},{"location":"references/sucata.scene/#sucatasceneunload_global","title":"sucata.scene.unload_global","text":"<p>Removes a globally stored entity by key.</p> <p>parameters</p> <ul> <li>key <code>string</code> - The key of the global entity to remove  </li> </ul>"},{"location":"references/sucata.time/","title":"sucata.time","text":"<p>The time module of the Sucata game engine.</p>"},{"location":"references/sucata.time/#timer","title":"Timer","text":"<p>Configuration table used to create timers.</p> <p>fields</p> <ul> <li> <p>time <code>number</code>   The duration of the timer in seconds.</p> </li> <li> <p>auto_start? <code>boolean</code>   Whether the timer should start automatically (default: <code>true</code>).</p> </li> <li> <p>one_shot? <code>boolean</code>   Whether the timer should only run once (default: <code>true</code>).</p> </li> <li> <p>loop? <code>boolean</code>   Whether the timer should loop (default: <code>false</code>).</p> </li> </ul>"},{"location":"references/sucata.time/#sucatatimeget_delta","title":"sucata.time.get_delta","text":"<p>Gets the delta time (time elapsed since last frame) in seconds, multiplied by the current time scale.</p> <p>return</p> <ul> <li>delta <code>number</code> - The delta time in seconds  </li> </ul>"},{"location":"references/sucata.time/#sucatatimeget_fps","title":"sucata.time.get_fps","text":"<p>Gets the current frames per second.</p> <p>return</p> <ul> <li>fps <code>number</code> - The current FPS  </li> </ul>"},{"location":"references/sucata.time/#sucatatimecreate_timer","title":"sucata.time.create_timer","text":"<p>Creates a timer with a callback function.</p> <p>parameters</p> <ul> <li>callback <code>function</code> - The function to call when the timer expires  </li> <li>config <code>number | Timer</code> - Timer duration in seconds, or a timer configuration table  </li> </ul> <p>return</p> <ul> <li>timer_id <code>number</code> - The ID of the created timer  </li> </ul>"},{"location":"references/sucata.time/#sucatatimepause_timer","title":"sucata.time.pause_timer","text":"<p>Pauses a timer.</p> <p>parameters</p> <ul> <li>timer_id <code>number</code> - The ID of the timer to pause  </li> </ul>"},{"location":"references/sucata.time/#sucatatimestop_timer","title":"sucata.time.stop_timer","text":"<p>Stops a timer.</p> <p>parameters</p> <ul> <li>timer_id <code>number</code> - The ID of the timer to stop  </li> </ul>"},{"location":"references/sucata.time/#sucatatimeget_time_scale","title":"sucata.time.get_time_scale","text":"<p>Gets the current time scale value.</p> <p>return</p> <ul> <li>time_scale <code>number</code> - The current time scale value  </li> </ul>"},{"location":"references/sucata.time/#sucatatimeset_time_scale","title":"sucata.time.set_time_scale","text":"<p>Sets the time scale.</p> <p>parameters</p> <ul> <li>time_scale <code>number</code> - The new time scale value  </li> </ul>"},{"location":"references/sucata.window/","title":"sucata.window","text":"<p>The window module of the Sucata game engine.</p>"},{"location":"references/sucata.window/#sucatawindowset_mouse_lock","title":"sucata.window.set_mouse_lock","text":"<p>Sets whether the mouse cursor is locked to the window.</p> <p>parameters</p> <ul> <li>locked <code>boolean</code> - Whether to lock the mouse cursor  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_mouse_lock","title":"sucata.window.get_mouse_lock","text":"<p>Gets whether the mouse cursor is locked to the window.</p> <p>return</p> <ul> <li>locked <code>boolean</code> - Whether the mouse cursor is locked  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_mouse_visible","title":"sucata.window.set_mouse_visible","text":"<p>Sets whether the mouse cursor is visible.</p> <p>parameters</p> <ul> <li>visible <code>boolean</code> - Whether the mouse cursor should be visible  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_mouse_visible","title":"sucata.window.get_mouse_visible","text":"<p>Gets whether the mouse cursor is visible.</p> <p>return</p> <ul> <li>visible <code>boolean</code> - Whether the mouse cursor is visible  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_window_title","title":"sucata.window.set_window_title","text":"<p>Sets the window title.</p> <p>parameters</p> <ul> <li>title <code>string</code> - The new window title  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_window_title","title":"sucata.window.get_window_title","text":"<p>Gets the current window title.</p> <p>return</p> <ul> <li>title <code>string</code> - The current window title  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_window_size","title":"sucata.window.set_window_size","text":"<p>Sets the window size.</p> <p>parameters</p> <ul> <li>width <code>number</code> - The new window width in pixels  </li> <li>height <code>number</code> - The new window height in pixels  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_window_size","title":"sucata.window.get_window_size","text":"<p>Gets the current window size.</p> <p>return</p> <ul> <li>width <code>number</code> - The current window width in pixels  </li> <li>height <code>number</code> - The current window height in pixels  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_fullscreen","title":"sucata.window.set_fullscreen","text":"<p>Sets whether the window is in fullscreen mode.</p> <p>parameters</p> <ul> <li>fullscreen <code>boolean</code> - Whether the window should be fullscreen  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_fullscreen","title":"sucata.window.get_fullscreen","text":"<p>Gets whether the window is in fullscreen mode.</p> <p>return</p> <ul> <li>fullscreen <code>boolean</code> - Whether the window is fullscreen  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_vsync","title":"sucata.window.set_vsync","text":"<p>Sets the vsync mode.</p> <p>parameters</p> <ul> <li>vsync <code>number</code> - The vsync mode (<code>0</code> = off, <code>1</code> = on, higher values for specific intervals)  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_vsync","title":"sucata.window.get_vsync","text":"<p>Gets the current vsync mode.</p> <p>return</p> <ul> <li>vsync <code>number</code> - The current vsync mode  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowquit","title":"sucata.window.quit","text":"<p>Quits the application.</p>"},{"location":"references/sucata.window/#sucatawindowshow_debug_info","title":"sucata.window.show_debug_info","text":"<p>Sets whether to show debug information.</p> <p>parameters</p> <ul> <li>show <code>boolean</code> - Whether to show debug information  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_keep_aspect","title":"sucata.window.set_keep_aspect","text":"<p>Sets whether to keep the aspect ratio with black bars.</p> <p>parameters</p> <ul> <li>keep <code>number</code> - Whether to maintain the aspect ratio  </li> <li><code>0</code> = off  </li> <li><code>1</code> = keep aspect with bars  </li> <li><code>2</code> = keep aspect with crop  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_keep_aspect","title":"sucata.window.get_keep_aspect","text":"<p>Gets whether the window keeps aspect ratio with black bars.</p> <p>return</p> <ul> <li>keep <code>number</code> - Whether aspect ratio is maintained  </li> <li><code>0</code> = off  </li> <li><code>1</code> = keep aspect with bars  </li> <li><code>2</code> = keep aspect with crop  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_window_icon","title":"sucata.window.set_window_icon","text":"<p>Sets the window icon from a file path.</p> <p>parameters</p> <ul> <li>path <code>string</code> - The file path to the icon image  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_window_icon","title":"sucata.window.get_window_icon","text":"<p>Gets the current window icon path.</p> <p>return</p> <ul> <li>path <code>string</code> - The file path of the current window icon  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowset_cursor","title":"sucata.window.set_cursor","text":"<p>Sets the mouse cursor appearance.</p> <p>parameters</p> <ul> <li>cursor_name <code>string</code> - The cursor style to use. Available values:</li> <li><code>\"default\"</code>, <code>\"arrow\"</code>, <code>\"ibeam\"</code>, <code>\"crosshair\"</code>, <code>\"pointing_hand\"</code></li> <li><code>\"resize_ew\"</code>, <code>\"resize_ns\"</code>, <code>\"resize_nwse\"</code>, <code>\"resize_nesw\"</code>, <code>\"resize_all\"</code></li> <li><code>\"not_allowed\"</code></li> <li><code>\"custom_0\"</code> through <code>\"custom_15\"</code></li> </ul>"},{"location":"references/sucata.window/#sucatawindowget_cursor","title":"sucata.window.get_cursor","text":"<p>Gets the current mouse cursor name.</p> <p>return</p> <ul> <li>cursor_name <code>string</code> - The name of the current cursor style  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowon_init","title":"sucata.window.on_init","text":"<p>Registers a callback to run when the window/game initializes.</p> <p>parameters</p> <ul> <li>callback <code>function</code> - The function to call on init  </li> </ul>"},{"location":"references/sucata.window/#sucatawindowon_exit","title":"sucata.window.on_exit","text":"<p>Registers a callback to run when the window/game is about to exit.</p> <p>parameters</p> <ul> <li>callback <code>function</code> - The function to call on exit  </li> </ul>"}]}